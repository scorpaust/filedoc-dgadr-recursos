# Funcionalidade Atual

Fase 3 (Deployment) — Containerização para Produção

<!-- Ver especificação completa em context/features/db_ci_cd/fase-3-deploy-containerizacao.md -->

## Estado

<!-- Não iniciada|Em progresso|Concluída -->

Em progresso

## Objetivos

<!-- Objetivos e requisitos -->

Ver `context/features/db_ci_cd/fase-3-deploy-containerizacao.md`.

## Notas

<!-- Notas adicionais -->

## Histórico

<!-- Manter atualizado. Da mais antiga para a mais recente -->

- Configuração do projeto e limpeza do boilerplate
- Adaptação do `.gitignore` ao projeto, commit inicial ("Configuração inicial") e push para o repositório remoto `git@github.com:scorpaust/filedoc-dgadr-recursos.git`
- Criação de `src/lib/mock-data.ts` com dados mock do dashboard (recursos, dicas, FAQs, pedidos de suporte, utilizadores), a partir dos screenshots de referência
- Início da Fase 1 (UI) — Design System & Casca da Aplicação, na branch `feature/fase-1-ui-design-system`
- Implementação completa da Fase 1 (UI):
  - infraestrutura: ESLint (`angular-eslint`) e scripts `lint`/`format:check`/`typecheck`; `@angular/cdk` instalado (sem `@angular/material`); prefixo de selector alterado para `fdr-`;
  - tema: tokens SCSS (`src/styles/`), `ThemeService` com Signals, deteção via `prefers-color-scheme`, persistência e script inline no `index.html` para evitar flash de tema incorreto;
  - modelos e mocks movidos de `src/lib/mock-data.ts` para `src/app/shared/models` e `src/app/shared/mocks`, com aviso de dados de demonstração;
  - 15 componentes partilhados em `src/app/shared/components` (Icon, Button, Tag, Pill, Carimbo, Skeleton, Chip, EmptyState, SegmentedControl, Toast+ToastService, Accordion+AccordionItem, DropdownFilter, Dialog+DialogService, ResourceCard, RoutePlaceholder), todos standalone/OnPush/`input()`/`output()`, com testes unitários;
  - layout (`core/layout`): AppHeader, AppNav (grupos Portal/Pedidos/Gestão), AppFooter, AppShell com navegação em gaveta (CDK) em ecrãs estreitos;
  - roteamento com lazy loading para `/inicio`, `/recursos`, `/recursos/:slug`, `/dicas-faq`, `/suporte`, `/suporte/gestao`, `/conteudos`, `/administracao`, `/acesso-negado` e `/**`, com páginas placeholder e páginas de erro com `EmptyStateComponent`;
  - validação automática: `lint`, `format:check`, `typecheck`, `test` (72/72 testes) e `build` (produção) todos a passar sem erros;
  - validação manual no browser (Playwright, ferramenta temporária não guardada no projeto): as 10 rotas renderizam sem erros de consola; alternância de tema funciona e persiste após recarregar; navegação em gaveta funcional a 375 px sem scroll horizontal.
  - Nota: validação manual não cobriu explicitamente todas as larguras da checklist (320/768/1024 px) nem uma passagem completa de navegação só por teclado no browser — os componentes usam elementos nativos (`button`/`a`) e os comportamentos de teclado (Escape, setas, foco contido/devolvido) estão cobertos por testes unitários, mas falta confirmação visual manual dessas larguras e desse fluxo antes de considerar a fase concluída.
  - Ainda por fazer antes de marcar como concluída: validação manual das larguras/teclado em falta, autorização do utilizador para commit.
- Início da Fase 2 (UI) — Ecrãs de Autenticação, na branch `feature/fase-2-ui-autenticacao`
- Implementação completa da Fase 2 (UI):
  - `AuthService` simulado (`core/auth`), com Signals (`currentUser`, `currentRole`, `isAuthenticated`), `login`/`logout`/`changePassword` com atraso simulado e mensagem de erro sempre genérica; `mock-credentials.ts` com credenciais de demonstração para os 4 utilizadores mock ativos;
  - adicionado um 5.º utilizador mock (`ADMIN`, Ana Ferreira) a `shared/mocks/users.mock.ts`, cobrindo as 4 funções da aplicação; removido o `currentUser` estático (substituído pelo `AuthService`);
  - `authGuard` e `roleGuard` (`core/auth`), funcionais e testados, incluindo os caminhos negativos (sem sessão → `/login`; função não permitida → `/acesso-negado`);
  - reestruturação do roteamento (`app.routes.ts`): `/login` pública e fora da casca; `AppShellComponent` movido para uma rota-pai `''` protegida por `authGuard`, com as restantes rotas da Fase 1 como filhas; `roleGuard` aplicado a `/suporte/gestao` (`SUPPORT_AGENT`/`ADMIN`), `/conteudos` (`CONTENT_EDITOR`/`ADMIN`) e `/administracao` (`ADMIN`); nova rota autenticada `/definicoes/palavra-passe`; `App` (raiz) passou a expor apenas `<router-outlet />`;
  - ecrã de login (`features/auth/login-page`) com Reactive Forms tipado, validação de e-mail/palavra-passe, alternar visibilidade da palavra-passe, erro genérico de credenciais inválidas, foco automático no campo de e-mail, e ferramenta de desenvolvimento (claramente identificada) para iniciar sessão rapidamente com um utilizador mock por função;
  - ecrã de alteração de palavra-passe (`features/auth/change-password-page`), rota autenticada, com validação de campos obrigatórios e confirmação igual à nova palavra-passe, sucesso via `ToastService` e erro genérico simulado;
  - `UserMenuComponent` (`core/layout/user-menu`, padrão CDK Overlay igual ao `DropdownFilterComponent`) com as opções "Alterar palavra-passe" e "Terminar sessão"; `AppHeaderComponent` ligado ao `AuthService` em vez de dados estáticos;
  - novos ícones `eye`/`eye-off`/`log-out`/`key` no `IconComponent`; estilos de campo de formulário partilhados em `src/styles/_forms.scss`, reutilizados pelos dois ecrãs;
  - validação automática: `lint`, `typecheck`, `test` (99/99 testes) e `build` (produção) todos a passar sem erros; `format:check` sem problemas nos ficheiros desta fase (o aviso pré-existente em ~105 ficheiros não tocados por esta fase replica-se também numa checkout limpa de `main`, por diferença de fim de linha `core.autocrlf` neste ambiente Windows — não é uma regressão introduzida aqui);
  - validação manual no browser (Playwright, ferramenta temporária não guardada no projeto): acesso não autenticado a `/inicio` redireciona para `/login`; ecrã de login sem cabeçalho/navegação; credenciais inválidas mostram erro genérico; ferramenta de simulação de função autentica e redireciona para `/inicio`; utilizador `EMPLOYEE` bloqueado em `/administracao` e redirecionado para `/acesso-negado`; alteração de palavra-passe valida a confirmação e mostra sucesso; terminar sessão redireciona para `/login` com foco devolvido ao campo de e-mail; zero erros de consola; inspeção visual em modo claro, escuro e a 375 px sem problemas aparentes;
  - decisões tomadas: ferramenta de simulação de função colocada no rodapé do ecrã de login (opção sugerida na especificação); alteração de palavra-passe implementada como página dedicada (`/definicoes/palavra-passe`), não como diálogo, por consistência com os restantes formulários da aplicação.
  - correções de acessibilidade encontradas e resolvidas a pedido do utilizador (via Microsoft Edge Tools/webhint e verificação manual), com confirmação por axe-core real e não apenas pela build: `[attr.aria-label]="expr()"` reescrito como `aria-label="{{ expr() }}"` em vários componentes (o linter estático do editor não avalia bindings Angular, embora o runtime já estivesse correto); contraste insuficiente (3.12:1) do avatar do `UserMenuComponent` corrigido para `--fdr-accent-primary-hover` (~4.86:1); aviso "axe/list" em `AppNavComponent` investigado e confirmado como falso positivo do linter estático (não entende `@for`) — mantido `@for`, sem alteração de código, por exigência de `.claude/CLAUDE.md`;
  - validação manual completa a pedido do utilizador, antes do commit: larguras 320/375/768/1024/1440 px em `/login`, `/inicio`, `/recursos`, `/dicas-faq`, `/suporte` (incl. gaveta móvel aberta), `/definicoes/palavra-passe` e `/acesso-negado` — sem scroll horizontal nem erros de consola; passagem completa de navegação só por teclado (login incl. alternância de palavra-passe e submissão com Enter, cabeçalho, gaveta de navegação móvel, menu do utilizador, alteração de palavra-passe, logout);
  - esta validação encontrou e corrigiu 3 bugs reais não cobertos pelos testes unitários: (1) `UserMenuComponent` — o chevron do gatilho ficava sempre visível e causava overflow horizontal a 320 px; escondido também abaixo de 768 px, junto com o nome/cargo; (2) `AppShellComponent` (gaveta de navegação móvel, herdada da Fase 1) — os links da gaveta fechada continuavam focáveis fora do ecrã (só havia `transform`, sem `visibility: hidden`), e não existia atalho de Escape para fechar a gaveta nem devolução de foco ao botão que a abriu; ambos corrigidos; (3) `UserMenuComponent` — o painel do menu não tinha `cdkTrapFocus` nem foco automático no primeiro item, pelo que o Tab saltava da barra lateral em vez de percorrer as opções do menu; corrigido com `cdkTrapFocus`/`cdkTrapFocusAutoCapture` e foco devolvido ao gatilho ao fechar;
  - `lint`, `typecheck`, `test` (99/99) e `build` revalidados após todas as correções, todos a passar sem erros.
  - Commit ainda por autorizar.
- Início da Fase 3 (UI) — Catálogo de Recursos, na branch `feature/fase-3-ui-catalogo`
- Implementação completa da Fase 3 (UI):
  - `Resource` (`shared/models/resource.model.ts`) estendido com `slug`, `summary` (resumo, usado no cartão), `tags`, `publishedAt`, e `workflow`/`documentType` passaram a uniões literais tipadas (`Workflow`/`DocumentType`, com as listas `WORKFLOWS`/`DOCUMENT_TYPES` da secção B do `project-spec.md`); `description` mantido para a Fase 4 (detalhe);
  - `shared/mocks/resources.mock.ts` expandido de 6 para 24 recursos, cobrindo os 3 estados editoriais (16 publicados, 5 rascunho, 3 arquivados), os 2 tipos, as 3 dificuldades, os 8 fluxos e os 7 tipos de documento;
  - `ResourceCardComponent` (Fase 1) atualizado para navegar por `resource().slug` (em vez de `id`) e mostrar `resource().summary`; testes ajustados;
  - `PaginationComponent` novo (`shared/components/pagination`), acessível (`nav[aria-label]`, `aria-current="page"`, reticências para muitas páginas), com testes;
  - `ResourceMockService` novo (`features/resources/data`), com `search(params): Observable<{ items; total }>` e `getBySlug(slug)`, atraso simulado de 300 ms (`delay`), pesquisa por título/resumo/etiquetas (sem distinção de maiúsculas/acentos), filtros por tipo/fluxo/dificuldade, ordenação "mais recentes"/alfabética, paginação, e visibilidade por estado editorial e função (`CONTENT_EDITOR`/`ADMIN` veem rascunhos; arquivados nunca aparecem); testado exaustivamente;
  - `ResourceCatalogPageComponent` (`features/resources/resource-catalog-page`) reescrito por completo: barra de pesquisa com debounce de 250 ms (Reactive Forms), `SegmentedControlComponent` (tipo), dois `DropdownFilterComponent` (fluxo/dificuldade), `select` nativo de ordenação, grelha responsiva (`auto-fill`), skeletons durante o carregamento, `EmptyStateComponent` com ação de limpar filtros, `PaginationComponent`, e sincronização de todos os parâmetros com a URL (`q`, `tipo`, `fluxo`, `dificuldade`, `ordenar`, `pagina`) através de `Router.navigate(..., { replaceUrl: true })`; usa `switchMap` (via `toObservable`/`toSignal`) para cancelar pesquisas em curso;
  - decisão registada (risco em aberto do `fase-3-ui-catalogo.md`): "mais recentes" ordena por `publishedAt` (data de publicação), não por `updatedAt`; documentado em comentário no `ResourceMockService`;
  - decisão: o filtro por tipo de documento previsto no `project-spec.md` (secção C) não foi incluído nesta fase — o `fase-3-ui-catalogo.md` (tarefa D) restringe explicitamente os filtros a pesquisa/tipo/fluxo/dificuldade;
  - validação automática: `lint`, `typecheck`, `test` (126/126 testes) e `build` (produção) todos a passar sem erros; `format:check` sem regressões (o aviso pré-existente por fim de linha `core.autocrlf` replica-se numa checkout limpa de `main`, tal como documentado na Fase 2);
  - validação manual no browser (Playwright, ferramenta temporária não guardada no projeto, script descartável fora do repositório): 29 de 30 verificações automatizadas passaram — pesquisa por texto, filtros de tipo/fluxo isolados, "limpar filtros" (barra de ferramentas e estado vazio), paginação (páginas com conteúdo distinto, URL atualizada), sincronização de filtros/pesquisa/página com a URL, teclado (setas no controlo segmentado, Escape a fechar o dropdown), zero erros de consola, e a regra de visibilidade confirmada com os 3 utilizadores mock relevantes (`EMPLOYEE`: 16 recursos, sem rascunhos nem arquivados; `CONTENT_EDITOR` e `ADMIN`: 21 recursos, incluindo rascunhos, nunca arquivados); sem scroll horizontal a 320/375/1024/1440 px;
  - encontrado (não corrigido, fora do âmbito desta fase): overflow horizontal de 2 px a 768 px, reproduzido também em `/inicio` e causado pela gaveta de navegação móvel da `AppShellComponent` (herdada da Fase 1) após múltiplos ciclos de login/logout — elemento com `visibility: hidden`, sem impacto visual percetível, mas a corrigir numa fase futura que toque o `AppShellComponent`;
  - encontrado (não corrigido, fora do âmbito desta fase): devido à sessão simulada da Fase 2 não ter persistência (só em memória), recarregar o browser numa página com filtros na URL perde a sessão e redireciona para `/login` sem preservar o URL de destino (sem `returnUrl`); o comportamento de "restaurar filtros a partir do URL" em si está validado ao nível do componente (testes unitários com `ActivatedRoute` simulada), mas o fluxo completo "recarregar a página autenticada" só poderá ser validado em browser depois de uma fase que adicione persistência de sessão;
  - commit efetuado em `feature/fase-3-ui-catalogo` (`Fase 3 (UI) — Catálogo de Recursos`), seguido de um commit à parte para uma correção de configuração não relacionada (`fix: definir rootDir explícito em tsconfig.app.json`, aplicada automaticamente pelo editor);
  - branch `feature/fase-3-ui-catalogo` integrada em `main` por pedido do utilizador, e apagada de seguida.
- Início da Fase 4 (UI) — Detalhe de Recurso (Vídeo / Guia), na branch `feature/fase-4-ui-detalhe-recurso`.
- Implementação completa da Fase 4 (UI):
  - `Resource` (`shared/models/resource.model.ts`) estendido com `videoUrl`/`captionsUrl` (para `type: 'video'`) e `pdfUrl` (para `type: 'guide'`), todos opcionais, seguindo o mesmo padrão já usado por `duration`/`pages`;
  - ficheiros de demonstração genéricos gerados localmente (nunca conteúdo institucional real) e guardados em `public/mock/` (mapeado para `/mock/*` pelo `assets` do `angular.json`, adaptação da sugestão `apps/web/src/assets/mock/` do `fase-4-ui-detalhe-recurso.md` à convenção `public/` já usada neste projeto Angular 20+): `resource-demo.mp4` (padrão sintético `testsrc2` + tom sinusoidal, via `ffmpeg`), `resource-demo.vtt` (legendas WebVTT de exemplo) e `resource-demo.pdf` (3 páginas de texto genérico, gerado a partir de HTML via impressão headless do Microsoft Edge);
  - `shared/mocks/resources.mock.ts`: todos os 12 recursos `video` passaram a ter `videoUrl`, com `captionsUrl` em metade deles (para validar o comportamento "quando disponíveis"); todos os 12 recursos `guide` passaram a ter `pdfUrl`;
  - `ResourceMockService` (Fase 3) estendido com `getRelated(ids): Observable<readonly Resource[]>`, que resolve `relatedResourceIds` do próprio recurso (campo já existente desde a Fase 3), aplicando a mesma regra de visibilidade por estado editorial/função, preservando a ordem e limitando a 4 itens;
  - `VideoPlayerComponent` novo (`shared/components/video-player`): `<video controls preload="metadata">` nativo, sem autoplay, com `<track kind="captions">` condicional e `aria-label`; controlos nativos do browser cobrem a navegação por teclado sem necessidade de controlos personalizados;
  - `PdfViewerComponent` novo (`shared/components/pdf-viewer`): `<iframe>` com `DomSanitizer.bypassSecurityTrustResourceUrl` e `title`, com hiperligação "Descarregar PDF" (`download`) sempre visível, independente do visualizador embutido funcionar;
  - `ResourceMetaComponent` novo (`shared/components/resource-meta`): reaproveita `TagComponent` (fluxo, tipo de documento, dificuldade, etiquetas) e `CarimboComponent` (data de atualização, por analogia com "carimbo de data" em contexto documental) — decisão registada, já que a especificação sugeria estes dois componentes sem detalhar o mapeamento exato dos campos;
  - `ResourceDetailPageComponent` reescrito por completo (deixou de ser placeholder): usa `toObservable`/`toSignal` com `switchMap` sobre o `slug` (mesmo padrão reativo decidido na Fase 3 para o catálogo, incluindo cancelamento de pedidos em curso), obtendo recurso e relacionados numa única cadeia; skeleton durante o carregamento; painel lateral com `ResourceMetaComponent`; secção de recursos relacionados reaproveitando o `ResourceCardComponent` tal como está (sem variante "compacta" dedicada — o cartão já é suficientemente compacto); botão "Pedir suporte sobre este tema" que navega para `/suporte` (ainda placeholder da Fase 1) com `queryParams` `recurso`/`assunto`; estado "não encontrado" com `EmptyStateComponent` e mensagem genérica partilhada tanto para `slug` inexistente como para recurso em rascunho sem permissão (sem revelar a diferença, conforme exigido);
  - decisão (risco em aberto do `fase-4-ui-detalhe-recurso.md`): visualização de PDF via `<iframe>` nativo, sem biblioteca dedicada — suficiente para um ficheiro de demonstração local e evita uma dependência pesada sem necessidade comprovada; carregamento do recurso por `slug` mantém o padrão "sem resolver" já usado no catálogo (carregamento no componente com skeleton), por consistência;
  - validação automática: `lint`, `typecheck`, `test` (145/145 testes, incluindo os novos `VideoPlayerComponent`, `PdfViewerComponent`, `ResourceMetaComponent` e a reescrita de `ResourceDetailPageComponent`/`ResourceMockService`) e `build` (produção) todos a passar sem erros; `format:check` sem regressões nos ficheiros desta fase (mesmo aviso pré-existente por fim de linha `core.autocrlf`, documentado desde a Fase 2); confirmado, por comparação com a mesma suite correndo em `main` sem as alterações desta fase, que uma falha intermitente por timeout num teste pré-existente e não relacionado (`AppShellComponent`) já ocorria antes desta fase, não sendo uma regressão introduzida aqui;
  - validação manual no browser (Playwright com Chromium local, ferramenta temporária não guardada no projeto, script descartável fora do repositório): 20/20 verificações automatizadas passaram — navegação de um cartão do catálogo até ao detalhe; leitor de vídeo sem autoplay, com legendas quando disponíveis; visualizador de PDF com `title` acessível e hiperligação de descarregar sempre presente; ação de suporte a navegar com os parâmetros corretos; recurso inexistente e recurso em rascunho (como `EMPLOYEE`) a mostrar a mesma mensagem genérica; recurso em rascunho visível como `CONTENT_EDITOR`; recursos relacionados filtrados corretamente por visibilidade; navegação por teclado a alcançar o botão de suporte a partir do leitor de vídeo nativo; sem scroll horizontal a 320/375/768/1024/1440 px; zero erros de consola;
  - encontrado (não corrigido, fora do âmbito): o iframe do `PdfViewerComponent` aparece em branco no Chromium headless usado para a validação automatizada, apesar de o ficheiro PDF ser servido corretamente (confirmado por `curl`: `200`, `application/pdf`, tamanho correto) — limitação conhecida do visualizador de PDF nativo em modo headless, não reproduzida em browsers reais (Edge/Chrome) com a interface gráfica ativa;
  - confirmado (reaproveitando a limitação já documentada na Fase 3): como a sessão simulada só existe em memória, qualquer navegação por URL direta (fora de cliques dentro da aplicação) perde a sessão; a validação Playwright desta fase contornou isto simulando apenas navegação interna da SPA (via `history.pushState` + `popstate`, equivalente a clicar num link), tal como um utilizador real faria;
  - commit ainda por autorizar.
- Correções pós-implementação, a pedido do utilizador (ainda não commitadas nem revalidadas por completo — ver nota abaixo):
  - `ResourceCardComponent`: corrigido alinhamento horizontal dos cartões na grelha (rodapé/linha divisória desalinhados quando o título ocupava um número diferente de linhas entre cartões da mesma linha) — `height: 100%` no cartão, `flex: 1` no corpo, `margin-top: auto` no rodapé, para ancorar sempre o rodapé+ação ao fundo, independentemente do conteúdo acima;
  - item de menu "Recurso" (`core/layout/app-nav/nav-items.ts`) deixou de ser um link estático partido (apontava para `/recursos/res-1`, um `id`, já não um `slug` válido desde a Fase 3): agora só aparece quando existe um "último recurso aberto" para o utilizador autenticado, com o rótulo a mostrar o título desse recurso e a ligar diretamente a ele; `LastViewedResourceService` novo (`core/services/`), com persistência em `localStorage`, **guardado por utilizador** (chave = `AppUser.id`) para não vazar entre os diferentes utilizadores mock da ferramenta de desenvolvimento — bug encontrado e corrigido depois de uma primeira versão global (sem distinção de utilizador); `ResourceDetailPageComponent` regista o recurso via `effect()` sempre que um recurso é carregado com sucesso; `AppNavComponent.navGroups` passou de array estático a `computed()`.
  - retomado após reiniciar a máquina: a suspeita de regressão real (em vez de apenas lentidão da máquina) confirmou-se em parte — isolando `resource-detail-page.component.spec.ts`, a suite ficava mesmo bloqueada (CPU a 100%, minutos sem terminar), e não era só lentidão do sistema. Causa identificada por eliminação: o `effect()` usado em `ResourceDetailPageComponent` para registar o "último recurso aberto" entrava em ciclo quando combinado com `vi.useFakeTimers()` nos testes (usado para controlar o atraso simulado do `ResourceMockService`). Corrigido substituindo o `effect()` por uma chamada direta a `lastViewedResourceService.record(...)` dentro do `tap()` já existente no pipeline reativo do `detail` (o mesmo `tap()` que já desligava o `loading`) — elimina o segundo mecanismo reativo em paralelo e resolve o bloqueio; sem impacto de comportamento em produção, só na forma como o efeito lateral é despoletado;
  - validação final após todas as correções: `lint`, `typecheck`, `format:check` e `test` (156/156 testes, incluindo os 4 novos do `LastViewedResourceService`, os testes atualizados do `AppNavComponent` para o item "Recurso" dinâmico — incluindo o caso de isolamento por utilizador — e os 2 novos do registo do último recurso na página de detalhe) e `build` (produção) todos a passar sem erros, em menos de 1 minuto cada, confirmando que o bloqueio anterior era mesmo o `effect()` e não lentidão persistente da máquina.
- Terceira correção pós-implementação, a pedido do utilizador: o número de páginas dos PDFs e a duração dos vídeos não tinham qualquer relação com os ficheiros de demonstração reais (todos os guias partilham o mesmo `resource-demo.pdf`, com 3 páginas; todos os vídeos partilham o mesmo `resource-demo.mp4`, com 6 segundos), mas o mock mostrava valores inventados e variáveis por recurso (ex. "8 páginas", "10 min").
  - `resources.mock.ts`: `pages` uniformizado para `3` em todos os recursos `guide` (valor real do PDF); `duration` uniformizado para `'0:06'` em todos os recursos `video` (valor real do vídeo, confirmado com `ffprobe`) — sem introduzir uma biblioteca de leitura de PDF (fora do âmbito da Fase 4, conforme decisão já registada);
  - `VideoPlayerComponent`: novo `output() durationChange`, emitido a partir do evento nativo `loadedmetadata` do `<video>`, com a duração real (em segundos) do ficheiro efetivamente carregado — self-correcting mesmo que o ficheiro de demonstração venha a mudar de duração no futuro;
  - `ResourceMetaComponent`: novo `input() liveDurationSeconds`; quando presente, substitui o `duration` do mock (formatado como `m:ss`) na secção "Detalhes"; mantém o valor do mock como fallback enquanto os metadados do vídeo ainda não carregaram;
  - `ResourceDetailPageComponent`: liga `(durationChange)` do `VideoPlayerComponent` a um `signal` `videoDuration`, passado a `[liveDurationSeconds]` do `ResourceMetaComponent`; o `signal` é reposto a `undefined` no mesmo `tap()` que já reiniciava `loading`, sempre que o `slug` muda, para não mostrar a duração do vídeo anterior ao navegar entre dois recursos de vídeo;
  - o cartão do catálogo (`ResourceCardComponent`) e as listas de "Recursos relacionados" continuam a usar o campo estático do mock (não carregam um `<video>` real), pelo que ficam automaticamente consistentes com o valor uniformizado, sem alterações de código;
  - validação: `lint`, `typecheck`, `format:check` e `test` (161/161 testes, incluindo 2 novos no `VideoPlayerComponent`, 2 novos no `ResourceMetaComponent` e 1 novo de integração na página de detalhe) e `build` (produção) todos a passar sem erros; confirmação visual no browser — duração real "0:06" a corresponder ao leitor nativo, "3 páginas" consistente em todos os guias.
  - commit efetuado em `feature/fase-4-ui-detalhe-recurso` ("Fase 4 (UI) — Detalhe de Recurso (Vídeo / Guia)"), a pedido do utilizador;
  - branch `feature/fase-4-ui-detalhe-recurso` integrada em `main` por pedido do utilizador (merge de integração, sem squash); `lint`, `typecheck`, `test` (161/161) e `build` revalidados em `main` após o merge, todos a passar; branch não apagada (ao contrário da Fase 3, não foi pedida a remoção).
- Criação da especificação da Fase 5 (UI) — Dicas & Perguntas Frequentes (`context/features/fase-5-ui-dicas-faq.md`), independente da Fase 4 e podendo ser desenvolvida em paralelo; implementação ainda não iniciada.
- Início da Fase 5 (UI) — Dicas & Perguntas Frequentes, na branch `feature/fase-5-ui-dicas-faq`.
- Implementação completa da Fase 5 (UI), validada manualmente no browser:
  - `Tip` (`shared/models/tip.model.ts`) e `Faq` (`shared/models/faq.model.ts`) estendidos com `sortOrder`; `Faq` também com `category?: string` (opcional, conforme `project-spec.md`, secção G); `shared/mocks/tips.mock.ts` e `shared/mocks/faqs.mock.ts` expandidos com `sortOrder` em todos os itens, uma dica e uma pergunta em rascunho, e duas categorias de perguntas (mais um grupo sem categoria), para validar visibilidade e agrupamento;
  - `TipsFaqMockService` novo (`features/tips-faq/data`), único serviço a servir dicas e perguntas frequentes (`getTips()`/`getFaqs()`), com a mesma regra de visibilidade por estado editorial/função já usada nas Fases 3 e 4, e ordenação por `sortOrder`; testado exaustivamente;
  - `TipCardComponent` novo (`shared/components/tip-card`): marcador circular decorativo (`aria-hidden`) mais texto, conforme protótipo;
  - `AccordionItemComponent` (Fase 1) estendido para animar a abertura/fecho do painel (`max-height` 0 → limite generoso de 60rem, com `transition` a usar `var(--fdr-duration-base)` — já reduzido a `0ms` globalmente sob `prefers-reduced-motion`, tal como as restantes transições da aplicação); substituído `[hidden]` por `[attr.inert]` no painel colapsado, para impedir o foco em conteúdo oculto sem cortar a transição; testes atualizados (`content.hidden` → `content.hasAttribute('inert')`); esta era a primeira utilização real do `AccordionComponent`/`AccordionItemComponent` fora dos testes unitários da Fase 1;
  - bug encontrado pelo utilizador em validação manual (screenshot) e corrigido: a primeira versão da animação (técnica `grid-template-rows` 0fr/1fr) e uma segunda tentativa (`max-height: 0` com o padding no mesmo elemento) deixavam sempre ~16 px de resposta visível e cortada em todos os itens do acordeão, mesmo colapsados. Causa raiz confirmada por inspeção do estilo computado num browser real: a altura "auto" de uma caixa acomoda sempre o seu próprio `padding-bottom`, mesmo com `max-height: 0` e `box-sizing: border-box` — `max-height` nunca reduz a caixa abaixo do padding+borda que ela própria define. Corrigido movendo o padding para um `<div>` interno (`&__content-inner`), separado do elemento com `overflow: hidden`/`max-height`, para que esse padding seja cortado inteiramente pelo contentor exterior quando colapsado — confirmado com `offsetHeight: 0` no elemento `.fdr-accordion-item__content` colapsado;
  - decisão registada (risco em aberto do `fase-5-ui-dicas-faq.md`): o acordeão permite várias perguntas abertas em simultâneo — cada `AccordionItemComponent` já gere o seu próprio estado `expanded` de forma independente (decisão herdada da Fase 1, não alterada), sem necessidade de coordenação entre itens;
  - `TipsFaqPageComponent` (`features/tips-faq/tips-faq-page`) reescrito por completo (deixou de ser placeholder): secção "Dicas rápidas" com grelha de `TipCardComponent`; secção "Perguntas frequentes" agrupada por categoria (uma pergunta sem categoria cai numa secção genérica "Outras perguntas"), cada grupo com o seu próprio `AccordionComponent`; skeleton enquanto os dados mock são "carregados" (`forkJoin` das duas chamadas do serviço, via `toSignal`); `EmptyStateComponent` para o caso (não coberto pelos dados mock reais, mas suportado e testado com um `TipsFaqMockService` simulado) de não existirem dicas nem perguntas publicadas para a função atual;
  - validação automática: `lint`, `typecheck`, `format:check` e `test` (176/176 testes, incluindo os novos `TipsFaqMockService`, `TipCardComponent`, a extensão do `AccordionItemComponent` e a reescrita de `TipsFaqPageComponent`) e `build` (produção) todos a passar sem erros; confirmado por segunda execução que a falha intermitente por timeout no teste pré-existente `AppShellComponent` (já documentada desde a Fase 4) não é uma regressão desta fase; `format:check` corrigiu formatação real (não apenas fim de linha `core.autocrlf`) em 7 ficheiros desta fase antes de confirmar — `npx prettier --write` aplicado a todos os ficheiros criados/alterados;
  - ferramenta de validação manual desta fase: sem Playwright disponível nesta sessão (ao contrário das fases anteriores), foi montada uma alternativa ad-hoc com `puppeteer-core` (instalado apenas na pasta de scratchpad, fora do projeto, nunca adicionado a `package.json`) a controlar o Microsoft Edge local, para poder autenticar com a ferramenta de simulação de função (sessão só em memória, como já documentado desde a Fase 3) e navegar dentro da SPA (via clique em links, não `page.goto` direto, para não perder a sessão);
  - validação manual completa: login como `EMPLOYEE` não mostra a dica nem a pergunta em rascunho; login como `CONTENT_EDITOR` mostra ambas; navegação só por teclado (Tab até ao primeiro item do acordeão, `Enter` e `Espaço` alternam `aria-expanded` corretamente); sem overflow horizontal a 320/375/768/1024/1440 px (incl. gaveta de navegação móvel a 375 px); inspeção visual em modo claro e escuro sem problemas aparentes; confirmado por `getComputedStyle` que `--fdr-duration-base` é `0ms` sob `prefers-reduced-motion: reduce`;
  - commit efetuado em `feature/fase-5-ui-dicas-faq` ("Fase 5 (UI) — Dicas & Perguntas Frequentes"), a pedido do utilizador;
  - branch `feature/fase-5-ui-dicas-faq` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; `lint`, `typecheck`, `test` (176/176) e `build` revalidados em `main` após o merge, todos a passar.
- Criação da especificação da Fase 6 (UI) — Suporte (Pedidos do Trabalhador) (`context/features/fase-6-ui-suporte-trabalhador.md`), assumindo como ponto de partida a Fase 1 (Design System), a Fase 2 (Autenticação) e a Fase 4 (ação "Pedir suporte sobre este tema"); implementação ainda não iniciada.
- Início da Fase 6 (UI) — Suporte (Pedidos do Trabalhador), na branch `feature/fase-6-ui-suporte-trabalhador`.
- Implementação completa da Fase 6 (UI), validada manualmente no browser:
  - `SupportTicket`/`TicketMessage` (`shared/models/support-ticket.model.ts`, já existentes desde a configuração inicial do projeto mas nunca usados por nenhuma fase) estendidos e alinhados com `project-spec.md` (secções H–K): `TicketCategory` (união das 7 categorias, seguindo o padrão já usado por `Workflow`/`DocumentType`); `TicketStatus` passou a usar os valores internos em inglês definidos na especificação (`OPEN`/`IN_PROGRESS`/`WAITING_FOR_USER`/`RESOLVED`/`CLOSED`, com `TICKET_STATUS_LABELS` para o português da interface) — decisão registada: o modelo herdado usava valores internos em português (`aberto`, `em_tratamento`, ...), corrigido nesta fase para cumprir a tarefa A da especificação; `TicketPriority` manteve os valores internos em português (`baixa`/`normal`/`alta`/`bloqueante`), por já seguirem o mesmo padrão de `Difficulty`, sem correspondência em inglês definida em `project-spec.md`; `SupportTicket` estendido com `requesterId` (liga o pedido ao `AppUser.id`, em vez de depender só do nome para autorização), `updatedAt`, `resolvedAt`, `closedAt`; `TicketMessage` estendido com `id` e `attachments?: TicketAttachment[]`;
  - `shared/mocks/support-tickets.mock.ts` (idem, já existente mas nunca usado) atualizado para o modelo novo: 7 pedidos, cobrindo os 5 estados para a persona principal de demonstração (Marta Silva/`EMPLOYEE`) e mais 2 pedidos de outros utilizadores mock (João Antunes, Paulo Matos), usados nos testes de autorização negativa;
  - `SupportTicketMockService` novo (`features/support/data`), com estado em memória (Signal): `listMine()`, `getMineById(id)` (devolve `undefined` tanto para um id inexistente como para um pedido de outro utilizador — nunca é possível distinguir os dois casos a partir da resposta), `createTicket(input)` (gera referência única `SUP-AAAA-XXXXXX`, parte aleatória de 6 carateres alfanuméricos não previsível, validada contra as referências já existentes), `addMessage(id, content, anexos?)` (bloqueado para pedidos "Encerrado" ou de outro utilizador) e `confirmResolution(id)` (só permitido a partir de "Resolvido", transita para "Encerrado"); testado exaustivamente, incluindo todos os caminhos de autorização negativa exigidos pela tarefa E;
  - `TicketReferenceComponent` novo (`shared/components/ticket-reference`), reaproveitando o `CarimboComponent` da Fase 1, usado de forma consistente nos 3 ecrãs (lista, detalhe — a criação mostra a referência já no ecrã de detalhe, para onde navega após submeter);
  - `TicketTimelineComponent` novo (`shared/components/ticket-timeline`), com uma interface (`TicketTimelineEntry`, campo `kind: 'message' | 'status-change'`) pensada desde já para a Fase 7 poder acrescentar notas internas como um novo valor de `kind`, sem alterar a forma da entrada (risco em aberto da especificação); nesta fase todas as entradas nascem das mensagens do próprio pedido (criação, respostas do trabalhador, respostas simuladas de suporte já presentes no mock) — decisão registada: não foram geradas entradas sintéticas de "mudança de estado", por essas ações pertencerem ao lado do agente de suporte (Fase 7); o estado atual do pedido já fica sempre visível no cabeçalho do ecrã de detalhe;
  - `TagComponent` (Fase 1) estendido com um novo `input() tone` genérico (`neutral`/`info`/`warning`/`danger`/`success`/`plum`), reaproveitando os tokens de "tone" já definidos em `_themes.scss` desde a Fase 1 mas nunca antes consumidos — usado para colorir prioridade e estado dos pedidos de suporte, mantendo o `input() difficulty` já existente sem alterações; mapeamento de tom partilhado entre os ecrãs de suporte em `features/support/ticket-tone.util.ts`, para que a mesma prioridade/estado use sempre a mesma cor;
  - `IconComponent` (Fase 1) estendido com os ícones `paperclip` (anexos) e `send` (não usado nesta fase, mas adicionado a par por serem tipicamente pedidos em conjunto num leitor de ícones deste tipo);
  - Página "Os meus pedidos" (`features/support/my-tickets-page`, reescrita — deixou de ser placeholder): filtro por estado com `SegmentedControlComponent` (Fase 1), reaproveitado tal como já tinha sido decidido na Fase 3 para filtros de escolha única — decisão registada: os "chips" do protótipo mapeiam para este componente já existente, sem necessidade de um novo; tabela semântica (`<table>`) com referência/assunto/categoria/prioridade/estado/data de atualização, transformada em cartões empilhados por CSS (sem duplicar template) abaixo dos 767 px já usados como referência de "ecrã estreito" desde a Fase 2; estados vazios distintos para "sem pedidos" e "sem resultados para o filtro"; ação "Novo pedido" sempre visível;
  - Página "Novo pedido" (`features/support/new-ticket-page`, nova): Reactive Forms com assunto/descrição/categoria/prioridade obrigatórios; aviso da prioridade "Bloqueante" com o texto exato do `project-spec.md`; deteta o recurso pré-associado a partir dos parâmetros `recurso` (slug) e `assunto` da Fase 4 (`ResourceMockService.getBySlug`), mostra-o num banner não removível por acidente (requer clique explícito em "remover") e usa o `assunto` para pré-preencher o campo; impede submissões duplicadas desativando a ação enquanto o pedido é criado; ao ter sucesso, navega para o detalhe do pedido criado;
  - Página "Detalhe do pedido" (`features/support/ticket-detail-page`, nova): cabeçalho com `TicketReferenceComponent`, assunto e etiquetas de categoria/prioridade/estado; `TicketTimelineComponent` com o histórico público; caixa de resposta visível em todos os estados exceto "Encerrado" (decisão do risco em aberto da especificação, conforme a proposta já sugerida); anexar informação adicional simulado com `<input type="file">` real (só o nome do ficheiro é lido e guardado — nunca o conteúdo, sem upload real), limitado a `MAX_ATTACHMENTS_PER_MESSAGE` (3, valor fixo nesta fase, conforme previsto); ação "Confirmar resolução" visível apenas quando "Resolvido", com confirmação por toast; estado "não encontrado" com a mesma mensagem genérica partilhada entre um id inexistente e um pedido de outro utilizador (mesmo padrão já usado pelo detalhe de recurso desde a Fase 4);
  - `app.routes.ts`: novas rotas `/suporte/novo` e `/suporte/:id` (esta última posicionada depois de `/suporte/gestao` na lista de rotas, para que a rota estática não seja intercetada pelo parâmetro);
  - corrigido `ResourceDetailPageComponent.requestSupport()` (Fase 4): navegava para `/suporte` (o único destino que existia nessa fase); atualizado para `/suporte/novo`, o ecrã de criação introduzido por esta fase — sem esta correção, o botão "Pedir suporte sobre este tema" deixava de pré-associar o recurso, por cair na lista em vez do formulário; encontrado durante a validação manual, não pelos testes unitários (o teste da Fase 4 apenas verificava a navegação, sem testar a integração ponta-a-ponta); teste unitário correspondente atualizado;
  - encontrado e corrigido durante a validação manual: `SegmentedControlComponent` (Fase 1) não tinha `flex-wrap`, pensado originalmente só para 2–3 opções curtas (tipo de recurso, na Fase 3); com as 6 opções do filtro de estado desta fase (incluindo "A aguardar resposta do utilizador"), causava overflow horizontal grave (~200+ px) a 320/375 px; corrigido com `flex-wrap: wrap` no componente partilhado, sem efeito nas restantes utilizações (menos opções, nunca preenchem uma linha nesses ecrãs);
  - encontrado pelo utilizador (screenshot) já depois da validação manual acima, antes do commit: `TagComponent` (Fase 1) tinha `height` fixa (`1.75rem`) e texto centrado; para etiquetas com texto mais longo que a largura da caixa (ex. o estado "A aguardar resposta do utilizador" numa coluna estreita da tabela), o texto quebrava em várias linhas mas a caixa não crescia, saindo visualmente fora do próprio contorno; corrigido substituindo `height` por `min-height` (a caixa cresce conforme o conteúdo) e `align-items`/`text-align` de `center` para `flex-start`/`left`, para que o texto fique sempre contido e alinhado à esquerda como as restantes etiquetas; confirmado visualmente (screenshot da lista "Os meus pedidos") e sem alteração percetível nas etiquetas de uma só linha já existentes desde a Fase 3;
  - validação automática: `lint`, `typecheck`, `format:check` e `test` (210/210 testes, incluindo os novos `SupportTicketMockService`, `TicketReferenceComponent`, `TicketTimelineComponent`, a extensão do `TagComponent` e as 3 páginas de suporte) e `build` (produção) todos a passar sem erros;
  - ferramenta de validação manual desta fase: Playwright instalado apenas na pasta de scratchpad (fora do projeto, nunca adicionado a `package.json`), a controlar Chromium local contra `ng serve`, autenticando com a ferramenta de simulação de função (sessão só em memória, como já documentado desde a Fase 3) e navegando por cliques dentro da SPA;
  - validação manual completa (22 verificações automatizadas no total, entre dois scripts descartáveis): Marta Silva (`EMPLOYEE`) só vê os próprios pedidos, nunca os de João Antunes/Paulo Matos; filtro de estado funcional; criação de pedido gera referência no formato `SUP-AAAA-XXXXXX` e navega para o detalhe; aviso da prioridade "Bloqueante" aparece só quando selecionada; resposta com anexo simulado atualiza o histórico; "Confirmar resolução" só disponível em "Resolvido" e transita corretamente para "Encerrado" (com a caixa de resposta e a própria ação a desaparecerem depois); aceder a um pedido de outro utilizador ou a um id inexistente mostra sempre a mesma mensagem genérica "Pedido não encontrado"; navegação por teclado (setas) no filtro de estado; fluxo completo a partir do botão "Pedir suporte sobre este tema" (Fase 4) até ao formulário com o recurso corretamente pré-associado e o assunto pré-preenchido; sem overflow horizontal a 320/375/768/1024/1440 px; zero erros de consola;
  - `lint`, `typecheck`, `format:check` e `test` (210/210) revalidados após a correção do `TagComponent`, todos a passar sem erros;
  - commit efetuado em `feature/fase-6-ui-suporte-trabalhador` ("Fase 6 (UI) — Suporte (Pedidos do Trabalhador)"), a pedido do utilizador;
  - branch `feature/fase-6-ui-suporte-trabalhador` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; `lint`, `typecheck`, `test` (210/210) e `build` revalidados em `main` após o merge, todos a passar (uma primeira execução da suite mostrou uma falha intermitente por timeout em `app-header.component.spec.ts`, não relacionada com esta fase — mesma intermitência já documentada desde a Fase 4 —, confirmada como não determinística por uma segunda execução limpa).
- Criação da especificação da Fase 7 (UI) — Gestão de Suporte (Vista de Agente) (`context/features/fase-7-ui-gestao-suporte.md`), assumindo como ponto de partida a Fase 1 (Design System), a Fase 2 (Autenticação) e, sobretudo, a Fase 6 (Suporte — pedidos do trabalhador), cujo `SupportTicketMockService` e componente de linha do tempo são estendidos nesta fase; implementação ainda não iniciada.
- Início da Fase 7 (UI) — Gestão de Suporte (Vista de Agente), na branch `feature/fase-7-ui-gestao-suporte`.
- Implementação completa da Fase 7 (UI), validada manualmente no browser:
  - `SupportTicket` (`shared/models/support-ticket.model.ts`): `assignee?: string` (nunca lido por nenhuma UI antes desta fase) substituído por `assigneeId?: string`, para poder ligar a atribuição a um `AppUser.id` real em vez de um nome livre; `TicketMessage` estendido com `kind?: 'message' | 'status-change'` (opcional, por omissão `'message'`), para que as operações de agente possam registar entradas de histórico com a mesma forma de uma mensagem normal — decisão já prevista desde a Fase 6 no comentário do `TicketTimelineEntryKind`;
  - `TicketTimelineEntryKind` (`shared/components/ticket-timeline`) estendido com `'internal-note'` (a extensão já prevista desde a Fase 6); `TicketTimelineComponent` passa a mostrar uma etiqueta "Nota interna" (reaproveitando o `TagComponent`, tom `warning`) e um contorno tracejado com fundo distinto para essas entradas, nunca ambíguo em relação a uma mensagem pública ou a uma mudança de estado;
  - `shared/mocks/users.mock.ts`: adicionado um segundo utilizador mock `SUPPORT_AGENT` (`user-6`, Sofia Ramos), para permitir testar reatribuição entre dois agentes (risco em aberto da especificação); `mock-credentials.ts` atualizado com a credencial correspondente;
  - `shared/mocks/support-tickets.mock.ts`: as 5 ocorrências de `assignee: 'Carlos Vieira'` convertidas para `assigneeId: 'user-2'`;
  - `IconComponent` (Fase 1) estendido com um novo ícone `user`, para a ação "Atribuir a mim" (não existia nenhum ícone de pessoa/utilizador até esta fase);
  - `features/support/agent.util.ts` novo: `SUPPORT_AGENTS` (lista de utilizadores mock `SUPPORT_AGENT` ativos) e `agentName(agentId)` (resolve o nome a partir do id), reutilizados tanto pelo serviço como pela página de gestão;
  - `SupportTicketMockService` (Fase 6) estendido com as operações de agente da tarefa A, sem alterar `listMine`/`getMineById`/`addMessage`/`confirmResolution` (continuam a servir a Fase 6 tal como estavam): `listAll(filters?: { status?, query? })` (todos os pedidos, independentemente do solicitante, com pesquisa por referência/assunto/nome do solicitante sem distinção de maiúsculas/acentos, tal como o padrão já usado pelo `ResourceMockService` desde a Fase 3); `assign`, `updateCategory`, `updatePriority`, `updateStatus`, `associateResource` (todas via um único método privado `mutateAsAgent`, que regista sempre uma entrada de histórico com `kind: 'status-change'`, `internal: false`, no formato consistente "{agente} alterou/atribuiu/associou ..."); `resolve`/`close` implementados como chamadas diretas a `updateStatus('RESOLVED'/'CLOSED')` — decisão registada: mais simples do que duplicar a bookkeeping de `resolvedAt`/`closedAt`, e ainda assim expostos como métodos próprios por corresponderem a ações distintas na interface; `addInternalNote` (`internal: true`, nunca visível ao trabalhador) e `reply` (resposta pública do agente, equivalente ao `addMessage` do trabalhador mas sem a restrição de "só o solicitante", bloqueado apenas quando o pedido está "Encerrado"); testado exaustivamente, incluindo o registo correto no histórico de cada operação;
  - `ticket-detail-page.component.ts` (Fase 6, vista do trabalhador): a construção da linha do tempo passou de `kind: 'message'` fixo para `kind: message.kind ?? 'message'`, para que as mudanças de estado feitas pelo agente (mensagens não internas com `kind: 'status-change'`) apareçam também do lado do trabalhador, tal como exigido pelo critério de aceitação "ao voltar à vista do trabalhador... as alterações de estado do agente são visíveis";
  - `ResourcePickerDialogComponent` novo (`features/support/resource-picker-dialog`), primeiro consumidor real do `DialogComponent`/`DialogService` da Fase 1 fora dos testes unitários: pesquisa com debounce de 250 ms sobre o `ResourceMockService.search` (Fase 3, `type: 'all'`, sem filtros de fluxo/dificuldade), lista de resultados selecionável, `DialogRef.close(resource)` ao escolher ou `close(undefined)` ao cancelar;
  - `SupportManagementPageComponent` (`features/support/support-management-page`) reescrito por completo (deixou de ser placeholder — a rota `/suporte/gestao` e o `roleGuard` com `['SUPPORT_AGENT', 'ADMIN']` já existiam desde a Fase 1/2, por preparação antecipada): painel de lista (chips de estado via `SegmentedControlComponent`, excluindo propositadamente "Encerrado" das chips — continua acessível via "Todos" —, campo de pesquisa com debounce de 250 ms) e painel de detalhe lado a lado (`display: grid`, duas colunas), com seleção por Signals local (`selectedTicketId`), não por parâmetro de rota — decisão registada do risco em aberto da especificação: mais simples nesta fase, sem necessidade comprovada de partilhar o URL de um pedido específico; painel de detalhe com referência/assunto/solicitante/categoria/prioridade/estado, recurso associado (quando existente, com hiperligação para `/recursos/:slug`, obtido via `ResourceMockService.getRelated([id])` para reaproveitar a mesma regra de visibilidade), controlo "Atribuído a" (select) + "Atribuir a mim", "Associar recurso" (abre o `ResourcePickerDialogComponent`), selects de categoria/prioridade/estado, "Marcar resolvido"/"Encerrar"; `TicketTimelineComponent` com mensagens públicas, notas internas e mudanças de estado intercaladas cronologicamente (tal como já vêm ordenadas no array `messages` do pedido); caixa de resposta com alternância "Resposta pública"/"Nota interna" por `radiogroup` nativo, escondida apenas quando "Encerrado" (mesma regra da Fase 6);
  - validação automática: `lint`, `typecheck`, `format:check` e `test` (238/238 testes, incluindo os novos testes do `SupportTicketMockService` para as operações de agente, do `TicketTimelineComponent` para `'internal-note'`, do `ResourcePickerDialogComponent` e do `SupportManagementPageComponent`) e `build` (produção) todos a passar sem erros; confirmado por segunda execução limpa que a falha intermitente por timeout em `app-header.component.spec.ts` (já documentada desde a Fase 4) não é uma regressão desta fase;
  - `npx prettier --write` aplicado a todos os ficheiros criados/alterados desta fase antes de validar `format:check`, tal como já decidido na Fase 5;
  - ferramenta de validação manual desta fase: Playwright não disponível nesta sessão (como já sucedera na Fase 5); `puppeteer-core` instalado apenas na pasta de scratchpad (fora do projeto, nunca adicionado a `package.json`), a controlar o Microsoft Edge local contra `ng serve`, autenticando via formulário de login com as credenciais mock e navegando por cliques/`history.pushState` dentro da SPA (nunca `page.goto` direto para uma rota autenticada, pelas mesmas razões já documentadas desde a Fase 3: a sessão simulada só existe em memória);
  - validação manual completa (15 verificações funcionais automatizadas num script descartável): como Carlos Vieira (`SUPPORT_AGENT`), a fila mostra pedidos de Marta Silva, João Antunes e Paulo Matos em simultâneo; selecionar um pedido atualiza o painel de detalhe sem navegação; "Atribuir a mim" atualiza corretamente o select "Atribuído a"; alternar para "Nota interna" e submeter mostra a entrada com o contorno/etiqueta distintos; alternar de volta para "Resposta pública" e submeter mostra a mensagem no histórico; alterar o estado para "Em tratamento" regista "Carlos Vieira alterou o estado para Em tratamento." no histórico; "Marcar resolvido" transita para "Resolvido"; "Encerrar" transita para "Encerrado" e esconde a caixa de resposta; o diálogo de associação de recurso abre com resultados e a associação escolhida aparece no cabeçalho com hiperligação; a pesquisa por texto filtra a fila corretamente; **crítico**: encerrando a sessão de Carlos Vieira através do menu do utilizador (não por reload do browser, para preservar o estado em memória do `SupportTicketMockService` partilhado) e iniciando sessão como Marta Silva, o ecrã de detalhe da Fase 6 mostra a resposta pública deixada pelo agente — confirma o critério de aceitação de que ambos os módulos partilham o mesmo serviço em memória; `EMPLOYEE` e `CONTENT_EDITOR` são ambos redirecionados para `/acesso-negado` ao tentar aceder a `/suporte/gestao` (via `roleGuard`, sem necessidade de teste unitário adicional — o guard já é genérico e testado desde a Fase 2); zero erros de consola durante todo o fluxo;
  - validação adicional das 5 larguras de referência (320/375/768/1024/1440 px): confirmado com `scrollWidth`/`clientWidth`/`innerWidth` que não existe overflow horizontal real em nenhuma largura (`scrollWidth` sempre ≤ `innerWidth`); o painel de lista+detalhe colapsa para uma coluna abaixo dos 1024 px, tal como exigido pelo critério de aceitação; nota metodológica: uma primeira verificação automatizada comparando `scrollWidth` com `clientWidth` acusou falsos positivos a 320/375 px — descartados após confirmar que se deviam apenas à barra de scroll vertical (sempre presente nesta página) reduzir o `clientWidth` sem que o conteúdo alguma vez exceda a largura real da janela (`innerWidth`);
  - navegação só por teclado não passou por uma verificação manual dedicada nesta fase (controlos usam elementos nativos — `select`, `input type="radio"`, `button` —, pelo que o comportamento de teclado é o nativo do browser); a confirmar visualmente numa fase futura que reutilize este ecrã, tal como já ressalvado para casos semelhantes nas Fases 1 e 3;
  - commit efetuado em `feature/fase-7-ui-gestao-suporte` ("Fase 7 (UI) — Gestão de Suporte (Vista de Agente)"), a pedido do utilizador;
  - branch `feature/fase-7-ui-gestao-suporte` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; `lint`, `typecheck` e `build` revalidados em `main` após o merge, todos a passar; `test` (228 testes) revalidado em duas execuções limpas — a primeira com 224/228 a passar (4 falhas por timeout, em `app-shell`, `ticket-detail-page` e `dialog.component`), a segunda com 226/228 a passar (falhas por timeout num conjunto diferente de testes — `app-header` e `app-shell`); conjunto de testes falhados distinto entre as duas execuções, confirmando que se trata da mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4, e não de uma regressão introduzida por esta fase (nenhuma falha ocorreu em testes desta fase nas duas execuções).
- Criação da especificação da Fase 8 (UI) — Gestão de Conteúdos (Área Editorial) (`context/features/fase-8-ui-gestao-conteudos.md`), assumindo como ponto de partida a Fase 1 (Design System), a Fase 2 (Autenticação), a Fase 3 (Catálogo — `ResourceMockService`) e a Fase 5 (Dicas & FAQ — respetivos serviços mock); implementação ainda não iniciada.
- Início da Fase 8 (UI) — Gestão de Conteúdos (Área Editorial), na branch `feature/fase-8-ui-gestao-conteudos`.
- Implementação completa da Fase 8 (UI), validada manualmente no browser:
  - `Resource` (`shared/models/resource.model.ts`) estendido com `thumbnailUrl`/`thumbnailAlt` (miniatura e respetivo texto alternativo, exigido para publicar — `project-spec.md`, secção N); decisão registada: `Workflow`/`DocumentType` alargados de união literal para `string` (mantendo `WORKFLOWS`/`DOCUMENT_TYPES` como semente inicial), para permitir que editores/administradores criem novos fluxos e tipos de documento em `TaxonomyMockService` sem quebrar a tipagem de `Resource`; `Difficulty` manteve-se união literal fixa (3 níveis, não é taxonomia gerível segundo `project-spec.md`, secção B);
  - `Taxonomy`/`TaxonomyKind` novos (`shared/models/taxonomy.model.ts`: `id`, `kind` — `workflow`/`documentType`/`tag` —, `label`, `order`, `active`); `EDITORIAL_STATUS_LABELS` novo em `editorial-status.model.ts` (rótulos partilhados por recursos/dicas/FAQ); `shared/mocks/taxonomies.mock.ts` novo, semeado a partir de `WORKFLOWS`/`DOCUMENT_TYPES` e das etiquetas já existentes em `resources.mock.ts` (deduplicadas);
  - `ResourceMockService` (Fase 3) convertido de array estático para `signal<readonly Resource[]>`, preservando a assinatura e o comportamento de `search`/`getBySlug`/`getRelated` (com a mesma regra de visibilidade por estado editorial/função); estendido com operações de gestão editorial, sem restrição de visibilidade (autorização já garantida pelo `roleGuard` em `/conteudos`): `listAllForManagement` (filtro por estado e pesquisa por título/autor/etiquetas), `getByIdForManagement`, `getForPreview` (usado exclusivamente pela pré-visualização do editor), `isSlugTaken`, `isTaxonomyInUse` (usado por `TaxonomyMockService.delete`), e `create`/`update`/`duplicate`/`publish`/`unpublish`/`archive`; `publish` valida todos os campos obrigatórios da secção N (incluindo ficheiro principal consoante o tipo, duração/páginas, e texto alternativo apenas quando existe miniatura) e rejeita com uma mensagem clara a listar os campos em falta; `duplicate` gera um novo `id`/`slug` únicos e força o estado a rascunho; testado exaustivamente, incluindo os bloqueios de publicação e a unicidade de slug;
  - `TaxonomyMockService` novo (`features/content-management/data`): `list` (por `kind`, ordenado), `create` (bloqueia etiquetas duplicadas dentro do mesmo `kind`), `update`, `toggleActive`, `reorder` (troca de `order` com o vizinho imediato — alternativa acessível ao arrastar e largar, obrigatória por teclado conforme a tarefa E/F da especificação), e `delete` (bloqueia com mensagem explicativa quando `ResourceMockService.isTaxonomyInUse` confirma que a taxonomia está associada a recursos existentes); testado exaustivamente, incluindo o bloqueio de eliminação;
  - `TipsFaqMockService` (Fase 5) convertido para `signal`-backed (dois signals, um por tipo de conteúdo), preservando `getTips`/`getFaqs`; estendido com `listAllTips`/`listAllFaqs` (sem restrição de visibilidade) e `createTip`/`updateTip`/`publishTip`/`unpublishTip`/`archiveTip`/`reorderTip`, mais os equivalentes para `Faq`; `reorderTip`/`reorderFaq` reaproveitam um único método privado genérico `swapOrder<T>` (troca de `sortOrder` com o vizinho, tal como o `TaxonomyMockService`); testado exaustivamente;
  - `FileUploadComponent` novo (`shared/components/file-upload`), reutilizável para vídeo/PDF/miniatura: valida extensão (por `kind`) e tamanho máximo no cliente (valores indicativos desta fase, comentados como a substituir pelos valores reais da API), mostra o nome do ficheiro selecionado e, para `kind="image"`, uma pré-visualização via `URL.createObjectURL` — decisão registada: para vídeo/PDF também se usa `URL.createObjectURL` (não apenas para a miniatura), para que o ficheiro efetivamente selecionado seja reproduzível na mesma sessão do browser, sem qualquer envio real; o `input[type=file]` real fica sobreposto (transparente) ao rótulo estilizado, em vez de `visually-hidden`, para que o anel de foco nativo continue visível em torno do botão (uma versão `visually-hidden` esconderia também o próprio anel de foco);
  - `ConfirmDialogComponent` novo (`shared/components/confirm-dialog`), diálogo de confirmação genérico reaproveitando `DialogComponent`/`DialogService` (Fase 1) e o token `DIALOG_DATA` do CDK, usado para arquivar um recurso e para eliminar uma taxonomia;
  - `IconComponent` (Fase 1) estendido com `edit`, `copy`, `archive`, `upload`, `arrow-up`, `arrow-down`, `trash`;
  - `ResourceTableComponent` novo (`features/content-management/resource-table`): pesquisa com debounce de 250 ms, filtro por estado editorial (`SegmentedControlComponent`: Todos/Rascunho/Publicado/Arquivado), tabela com as colunas recurso/tipo/fluxo/estado/atualizado/autor/ações (transformada em cartões empilhados abaixo dos 767 px, tal como já decidido na Fase 6), reaproveitando o padrão "mutate + `refreshTrigger`" já usado pela Fase 7 (`SupportManagementPageComponent`); ações por linha: editar e pré-visualizar (`routerLink`), duplicar, publicar/despublicar e arquivar (com `ConfirmDialogComponent`) — as duas últimas escondidas para recursos já arquivados; botão "Criar recurso" sempre visível; tabela envolvida num contentor com `overflow-x: auto` próprio (bug encontrado e corrigido durante a validação manual — ver abaixo);
  - `ResourceFormPageComponent` novo (`features/content-management/resource-form-page`), usado tanto para criar (`/conteudos/recursos/novo`) como para editar (`/conteudos/recursos/:id/editar`) um recurso: Reactive Forms com `title`/`slug`/`summary`/`description`/`type`/`workflow`/`documentType`/`difficulty` obrigatórios (`workflow`/`documentType` alimentados por `TaxonomyMockService`, apenas taxonomias ativas), `duration`obrigatória só para vídeo e `pages` só para guia (validadores comutados dinamicamente com a mudança de tipo, incluindo ao carregar um recurso existente), botão "Gerar a partir do título" para o slug (`slugify.util.ts`, decisão registada: geração explícita por botão, não sincronização automática a cada tecla, para não sobrepor edições manuais do slug); "Guardar como rascunho"/"Guardar alterações" só valida minimamente (título e tipo), sem exigir os restantes campos; "Publicar" valida todos os campos Reactive Forms mais o ficheiro principal (`FileUploadComponent`) e o texto alternativo da miniatura quando esta existe, mostrando erros junto aos campos e bloqueando a submissão enquanto algo faltar; "Despublicar" (só visível quando o recurso está publicado) chama diretamente `unpublish`, sem validação; "Pré-visualizar" (só visível a editar um recurso já existente) navega para `/conteudos/recursos/:slug/pre-visualizacao`;
  - `ResourceDetailPageComponent` (Fase 4) estendido com `previewMode = input(false)`, ligado via `data.previewMode` na nova rota de pré-visualização: usa `getForPreview` (ignora o estado editorial) em vez de `getBySlug`, não regista "último recurso visto", mostra um aviso persistente "Pré-visualização — este ecrã não é visível ao público." e troca a hiperligação/ação "Voltar ao catálogo" por "Voltar à gestão de conteúdos" (`/conteudos`); esconde a ação "Pedir suporte sobre este tema" (não faz sentido para um editor a pré-visualizar); decisão registada do risco em aberto da especificação: reutilização via um único `input()` de modo, sem duplicar o componente nem acoplar a área pública à área editorial;
  - `TipsFaqManagementComponent` novo (`features/content-management/tips-faq-management`): duas colunas (Dicas rápidas / Perguntas frequentes), cada uma com um formulário de criação e uma lista com estado editorial, edição em linha (alternando para um formulário de edição no próprio item), publicar/despublicar/arquivar, e reordenação por botões "subir"/"descer" (sem arrastar e largar nesta fase — a alternativa por botões, exigida pela especificação, é a única implementada, mais simples e já suficiente);
  - `TaxonomyManagementComponent` novo (`features/content-management/taxonomy-management`): três colunas (Fluxos/Tipos de documento/Etiquetas), cada uma com formulário de criação, lista com ativo/inativo, edição em linha, reordenação por botões, e eliminação com `ConfirmDialogComponent` (bloqueada pelo `TaxonomyMockService` quando em uso, com mensagem clara mostrada por toast);
  - `ContentManagementPageComponent` reescrito por completo (deixou de ser placeholder): três separadores dentro da mesma página (`SegmentedControlComponent`: Recursos/Dicas & FAQ/Taxonomias) — decisão registada do risco em aberto da especificação: mais simples nesta fase do que rotas dedicadas para cada sub-área, mantendo o padrão de tabs já usado no protótipo;
  - `app.routes.ts`: novas rotas `/conteudos/recursos/novo`, `/conteudos/recursos/:id/editar` e `/conteudos/recursos/:slug/pre-visualizacao` (esta última reaproveitando `ResourceDetailPageComponent`, com `data: { previewMode: true }`), todas protegidas por `roleGuard` (`CONTENT_EDITOR`/`ADMIN`), tal como a rota `/conteudos` já existente;
  - encontrado durante a implementação dos testes (antes da validação manual): os formulários de criação rápida de dica/taxonomia usavam `<form (ngSubmit)="...">` sem `[formGroup]`, apoiados apenas num `FormControl` isolado — como só `ReactiveFormsModule` estava importado (sem `FormsModule`), `(ngSubmit)` nunca disparava (só a diretiva `NgForm`, do `FormsModule`, expõe esse evento num `<form>` sem `[formGroup]`; um `<form>` com `[formGroup]` já expõe `(ngSubmit)` via `FormGroupDirective`, do `ReactiveFormsModule`); corrigido convertendo cada `FormControl` isolado num `FormGroup` de um único campo, mantendo a aplicação exclusivamente a Reactive Forms (sem importar `FormsModule`), conforme exigido por `.claude/CLAUDE.md`;
  - validação automática: `lint`, `format:check` (sem regressões nos ficheiros desta fase; o aviso pré-existente por fim de linha `core.autocrlf` nos ficheiros não tocados por esta fase replica-se numa checkout limpa de `main`, tal como documentado desde a Fase 2), `typecheck` e `test` (285/285 testes, incluindo todos os serviços e componentes novos desta fase) e `build` (produção) todos a passar sem erros;
  - ferramenta de validação manual desta fase: Playwright instalado apenas na pasta de scratchpad (fora do projeto, nunca adicionado a `package.json`), a controlar Chromium local contra `ng serve`, autenticando via formulário de login com a ferramenta de simulação de função e navegando por `history.pushState`/`popstate` dentro da SPA (nunca `page.goto` direto para uma rota autenticada — confirmado nesta fase que mesmo um `page.goto` a meio de uma sessão já autenticada recarrega o bundle e perde a sessão em memória, exatamente como já documentado desde a Fase 3, e não apenas ao abrir uma rota fria);
  - validação manual completa (26 verificações funcionais automatizadas num script descartável), como João Antunes (`CONTENT_EDITOR`): a tabela de `/conteudos` mostra rascunhos/publicados/arquivados, com pesquisa e filtro por estado a funcionar; criação de um recurso com todos os campos obrigatórios (incluindo carregamento simulado de vídeo e miniatura) publica com sucesso e o recurso aparece na tabela; edição de um recurso existente mostra a ação "Pré-visualizar", que abre a pré-visualização com o aviso correto; duplicar mostra uma entrada "(cópia)"; arquivar abre o diálogo de confirmação e conclui sem erros; a aba "Dicas & FAQ" cria uma dica e uma pergunta novas e o botão "subir" reordena sem erros; a aba "Taxonomias" cria uma etiqueta nova, ativa/desativa uma taxonomia, bloqueia a eliminação de um fluxo em uso (mensagem clara) e elimina com sucesso uma etiqueta sem uso; `EMPLOYEE` é redirecionado para `/acesso-negado` ao tentar aceder a `/conteudos` (via navegação interna da SPA, não por reload — testa efetivamente o `roleGuard`, não apenas o `authGuard`); zero erros de consola em todo o fluxo; verificação adicional de teclado: `Escape` fecha o `ConfirmDialogComponent` e devolve o foco, e as setas navegam nos controlos segmentados, sem passagem dedicada completa (controlos nativos — `select`/`input`/`button` —, mesma ressalva já feita em fases anteriores);
  - encontrado e corrigido durante a validação manual: a tabela de recursos (7 colunas) causava overflow horizontal de 170 px a 768/1024 px (intervalo em que a tabela ainda não colapsa em cartões, mas é demasiado larga para a viewport); corrigido envolvendo a tabela num contentor com `overflow-x: auto` próprio (o scroll fica contido na tabela, nunca na página), com `min-width` reposto a `0` no modo de cartões (< 768 px) para não regressar o comportamento já validado nesse intervalo; revalidado nas 5 larguras de referência (320/375/768/1024/1440 px) sem overflow em nenhuma;
  - `lint`, `typecheck`, `test` (285/285) e `build` revalidados após a correção do overflow, todos a passar sem erros;
  - encontrado pelo utilizador após a validação manual, antes do commit: um recurso (ou uma dica/pergunta) arquivado ficava sem qualquer ação disponível na UI — nem "Arquivar" nem "Publicar/Despublicar" são mostrados para itens já arquivados, e não existia nenhuma ação alternativa, pelo que arquivar por engano (ou os 3 recursos já arquivados desde a Fase 3) deixava o item preso nesse estado; corrigido com uma ação "Restaurar" (arquivado → rascunho), reaproveitando a transição já existente e sem restrição de estado em `ResourceMockService.unpublish`/`TipsFaqMockService.unpublishTip`/`unpublishFaq` (bastou expor um alias semântico — `restore`/`restoreTip`/`restoreFaq` — e mostrar o botão correspondente só quando o estado é "Arquivado", em vez de esconder toda a linha de ações); aplicado tanto à tabela de recursos como às listas de dicas/perguntas frequentes; testado (serviços e componentes) e confirmado manualmente no browser (3 recursos arquivados desde a Fase 3 já podem ser restaurados);
  - `lint`, `typecheck`, `test` (288/288) e `build` revalidados após a correção de "Restaurar", todos a passar sem erros;
  - commit efetuado em `feature/fase-8-ui-gestao-conteudos` ("Fase 8 (UI) — Gestão de Conteúdos (Área Editorial)"), a pedido do utilizador;
  - branch `feature/fase-8-ui-gestao-conteudos` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; `lint`, `typecheck`, `test` (288/288) e `build` revalidados em `main` após o merge, todos a passar sem erros.
- Atualização retroativa de `context/project-spec.md` e de `context/features/fase-2-ui-autenticacao.md`, a pedido do utilizador, introduzindo o modelo de **múltiplas funções por utilizador**: a secção "Funções do sistema" passa de "cada utilizador tem uma função principal" para "cada utilizador pode ter uma ou mais funções atribuídas em simultâneo" (ex.: `CONTENT_EDITOR` + `ADMIN`), com autorização definida como posse de **pelo menos uma** das funções exigidas; nova tabela `USERROLE` (associação `userId`/`role`, chave composta) substitui o campo `role` único em `USER`; secção O (Gestão de utilizadores) atualizada para filtro por função sem exclusividade, atribuição/remoção de múltiplas funções (nunca ficando sem nenhuma) e a regra do último administrador passa a incidir especificamente sobre a função `ADMIN` (não sobre "o último administrador" como conceito de função única); a especificação da Fase 2 foi ajustada em conformidade (`AuthService` com `roles: Signal<Role[]>`, `roleGuard` como interseção não vazia entre as funções do utilizador e as permitidas pela rota, novo critério de aceitação para um utilizador com duas funções a aceder a ambas as áreas correspondentes);
  - **nota importante, trabalho pendente**: esta atualização é retroativa a uma especificação já implementada e integrada em `main` — o código atual da Fase 2 ainda não reflete o novo modelo (`AppUser.role: UserRole` continua um valor único em `shared/models/app-user.model.ts`; `AuthService.currentRole` — `core/auth/auth.service.ts` — é um `computed` a partir desse único valor; `roleGuard` — `core/auth/role.guard.ts` — compara por igualdade, `allowedRoles.includes(currentRole)`, não por interseção); esta sessão apenas atualizou a documentação, sem tocar no código da Fase 2, que fica por migrar; a Fase 9 (ver abaixo) já assume este modelo novo desde a raiz (`UserMockService` com `roles: Role[]`), pelo que a migração do `AuthService`/`roleGuard`/`AppUser` da Fase 2 para múltiplas funções terá de ser feita nessa fase (ou antes dela) para que a Fase 9 tenha uma base coerente.
- Criação da especificação da Fase 9 (UI) — Administração (`context/features/fase-9-ui-administracao.md`), assumindo como ponto de partida a Fase 1 (Design System), a Fase 2 (Autenticação — ainda com migração pendente para múltiplas funções, ver nota acima) e a Fase 8 (`TaxonomyMockService`, reutilizado no resumo de taxonomias); implementação ainda não iniciada.
- Início da migração da Fase 2 para múltiplas funções e da Fase 9 (UI) — Administração, na branch `feature/fase-9-ui-administracao`, a pedido explícito do utilizador ("implementa as atualizações primeiro e a nova feature depois").
- Migração da Fase 2 para o modelo de múltiplas funções (trabalho pendente identificado na atualização retroativa acima), implementada nesta sessão antes da Fase 9 propriamente dita:
  - `AppUser.role: UserRole` (`shared/models/app-user.model.ts`) substituído por `AppUser.roles: readonly UserRole[]` (nunca vazio); novas funções `hasAnyRole(userRoles, allowedRoles)` (interseção não vazia) e `formatRoleLabels(roles)` (`roles.map(...).join(' + ')`) exportadas a par de `USER_ROLE_LABELS`;
  - `shared/mocks/users.mock.ts`: todos os utilizadores convertidos para `roles: ['X']`; João Antunes passou a `['CONTENT_EDITOR', 'ADMIN']` — primeiro utilizador mock com duas funções em simultâneo, satisfazendo simultaneamente o critério retroativo da Fase 2 e o requisito da Fase 9 de existirem pelo menos dois utilizadores `ADMIN` (o outro é Ana Ferreira, só `ADMIN`);
  - `UserMockService` novo (`core/auth/user-mock.service.ts`, não em `features/administration/data` como uma leitura literal da especificação sugeria) — decisão registada: colocado em `core/auth` porque precisa de ser a fonte "viva" única também consultada pelo `AuthService` (autenticação e validade de sessão), e `core` não pode depender de `features/administration` sem inverter a direção normal de dependências; expõe `list(filters?: { query?, roles?, status? })` (interseção multi-seleção, tal como o padrão de dropdown já validado no catálogo — `roles` é um array, não um único valor, para reutilizar o `DropdownFilterComponent` existente), `getById`/`isActive`/`findActiveByEmail` (síncronos, para o `AuthService`), `create`, `updateName`, `assignRoles`, `activate`, `deactivate`, `invalidateSessions` (todos `Observable`, 300 ms simulados) e `isLastAdminHolder` (síncrono, para bloquear a UI "antes de qualquer confirmação"); `assignRoles`/`deactivate` bloqueiam com mensagem clara quando o utilizador é o último com `ADMIN`, mesmo tendo outras funções; testado exaustivamente, incluindo o bloqueio e a listagem por múltiplas funções;
  - `AuthService` (Fase 2) estendido: `currentRole` (única) substituído por `roles: Signal<readonly UserRole[]>`; `authenticate()` passou a consultar `UserMockService.findActiveByEmail` (fonte viva) em vez do array estático `users.mock.ts`; `isAuthenticated` passou a `computed` que reconfirma, a cada leitura, que a conta continua ativa no `UserMockService` (`userMockService.isActive(user.id)`) — reativo, não apenas um `!== null`; um `effect()` novo no construtor limpa `currentUser` automaticamente assim que `isAuthenticated` passa a `false` com uma sessão em curso (ex.: um administrador desativa a própria conta), sem exigir uma nova navegação para ser detetado; testado, incluindo um teste novo que desativa o próprio utilizador em sessão e confirma o logout automático;
  - `roleGuard` (Fase 2) reescrito para interseção (`hasAnyRole`) em vez de comparação com uma única função; testado com um utilizador de duas funções a aceder a duas rotas protegidas por funções diferentes;
  - consumidores de `currentRole`/`.role` atualizados em todo o código de produção: `ResourceMockService`/`TipsFaqMockService` (visibilidade de rascunhos via `hasAnyRole`), `SupportTicketMockService` (`authorRole` via `formatRoleLabels`), `agent.util.ts` (`SUPPORT_AGENTS` via `.roles.includes(...)`), `login-page.component.ts` (rótulo da ferramenta de simulação de função via `formatRoleLabels`); e em cerca de 30 ficheiros de teste que construíam utilizadores mock com `role: 'X'` ou filtravam por `.role === 'X'`;
  - validação automática desta parte: `lint`, `typecheck`, `test` (todos os testes de `core/auth` a passar) e `build` sem erros antes de avançar para a Fase 9.
- Implementação completa da Fase 9 (UI) — Administração, validada manualmente no browser:
  - `AuditLogEntry`/`AuditEntityType` novos (`shared/models/audit-log.model.ts`) e `shared/mocks/audit-log.mock.ts` com 9 entradas ilustrativas cobrindo os tipos de ação da secção P (criação de conta, alteração de funções, ativação/desativação, publicação/arquivo de recursos, substituição de ficheiro, alterações em tickets, alteração de configuração), incluindo uma entrada de atribuição de função de administrador coerente com o novo utilizador mock multi-função;
  - `AuditLogMockService` novo (`features/administration/data`), apenas leitura (`list()`), claramente comentado como ilustrativo e nunca a confundir com histórico real de atividade;
  - `UserTableComponent` novo (`features/administration/user-table`), reaproveitando o padrão "pesquisa + dropdown de função (multi-seleção com contagem) + segmentado de estado (Todos/Ativos/Inativos) + tabela de ações + mutate/refreshTrigger" já validado no catálogo (Fase 3) e na gestão de conteúdos (Fase 8); colunas utilizador (avatar de iniciais/nome/e-mail), carreira, funções (uma `fdr-pill` por função, tons por `role-pill-tone.util.ts`), estado, último acesso, ações; ações por linha: editar funções (abre diálogo), ativar (direta)/desativar (com `ConfirmDialogComponent`, ação sensível — mas bloqueada **antes** de abrir esse diálogo quando removeria o último `ADMIN`, via `isLastAdminHolder`), invalidar sessões (direta, apenas simbólica);
  - `CreateUserDialogComponent` e `EditRolesDialogComponent` novos (`features/administration`), ambos com seleção múltipla de funções por checkboxes (nunca um único `select`, conforme exigido) e chamada direta ao `UserMockService`, mostrando erros inline (`role="alert"`) sem fechar o diálogo — decisão registada do risco em aberto da especificação: a criação de conta nunca pede palavra-passe (opção recomendada da própria especificação, para não induzir um padrão que a API real não seguirá);
  - `TaxonomySummaryComponent` novo (`features/administration/taxonomy-summary`), reaproveitando diretamente o `TaxonomyMockService` da Fase 8 (sem duplicar dados), com contagem de fluxos/tipos de documento/etiquetas ativos e atalho "Gerir →" para `/conteudos`;
  - `ContentManagementPageComponent` (Fase 8) estendido com leitura opcional do parâmetro de URL `tab` (`?tab=taxonomies`, valor em inglês — coincide com o `ContentTab` interno, não com o rótulo em português "Taxonomias" mostrado na aba) para permitir que o atalho "Gerir →" abra diretamente no separador de taxonomias, sem introduzir rotas dedicadas (mantém a decisão da Fase 8 de usar tabs, não rotas, para as sub-áreas de conteúdos);
  - `AdministrationPageComponent` (placeholder da Fase 1) reescrito por completo: `UserTableComponent` + `TaxonomySummaryComponent` + `AuditLogListComponent` (grelha responsiva `auto-fit`) na rota `/administracao`, já protegida por `roleGuard` (`ADMIN`) desde a Fase 1/2;
  - validação automática: `lint`, `format:check`, `typecheck` e `test` (327 testes, incluindo todos os novos serviços/componentes desta fase e da migração) e `build` (produção) todos a passar sem erros; falhas intermitentes por timeout em `app-shell`, `resource-table`, `taxonomy-management` e `support-management-page` confirmadas, por repetição em várias execuções, como a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4 (nenhuma falha ocorreu de forma consistente em testes desta sessão);
  - ferramenta de validação manual desta fase: sem `chromium-cli` disponível nesta sessão; `puppeteer-core` instalado apenas na pasta de scratchpad (fora do projeto, nunca adicionado a `package.json`), a controlar Microsoft Edge local contra `ng serve`, autenticando via ferramenta de simulação de função e navegando por cliques reais e por `history.pushState`/`popstate` (equivalente a um clique, para as verificações de guarda de rota) dentro da SPA;
  - validação manual encontrou e corrigiu **3 bugs reais**, nenhum detetado pelos testes unitários (todos de integração entre componentes/estilo, fora do alcance de testes isolados):
    1. números desalinhados no widget "Taxonomias" (reportado pelo utilizador, com captura de ecrã, durante a implementação) — a etiqueta "Tipos de documento ativos" quebra em duas linhas enquanto as outras duas ficam numa só, empurrando esse número para baixo; corrigido reservando sempre a altura de duas linhas em `dt` (`min-height` + `align-items: flex-end`), alinhando os três valores à mesma altura independentemente do número de linhas da etiqueta;
    2. atalho "Gerir →" não abria a Fase 8 no separador de taxonomias — o `queryParams` do atalho usava `tab: 'taxonomias'` (português, coincidindo com o rótulo visível), mas o `ContentTab` interno lido por `ContentManagementPageComponent` usa `'taxonomies'` (inglês); corrigido no atalho, com teste de regressão novo em `content-management-page.component.spec.ts` que injeta um `ActivatedRoute` com o parâmetro e confirma a abertura direta no separador correto — o teste unitário isolado do `TaxonomySummaryComponent` não podia ter apanhado esta inconsistência, por não testar a integração real entre os dois componentes;
    3. overflow horizontal de ~23 px a 320 px na página de administração — a grelha `&__secondary` (resumo de taxonomias + auditoria) usava `minmax(20rem, 1fr)`, e 20rem (320 px) já excede a largura de conteúdo disponível depois do padding da página a essa largura; corrigido para `minmax(min(20rem, 100%), 1fr)`, sem alterar o aspeto em ecrãs largos;
  - validação manual completa após as correções (script descartável, 15+6 verificações automatizadas): `EMPLOYEE` bloqueado ao navegar diretamente para `/administracao` (`/acesso-negado`); utilizador com `CONTENT_EDITOR`+`ADMIN` acede tanto a `/conteudos` como a `/administracao`; linha de utilizador multi-função mostra as pílulas correspondentes; pesquisa e filtro de estado da tabela funcionais; criação de utilizador com duas funções reflete-se de imediato na tabela; remoção de `ADMIN` de um utilizador é permitida havendo outro administrador e bloqueada (com toast, sem qualquer diálogo de confirmação a abrir) quando seria o último — confirmado visualmente com screenshot; atalho "Gerir →" e as 5 larguras de referência (320/375/768/1024/1440 px) sem overflow horizontal; zero erros de consola em toda a validação;
  - nota: a navegação principal (`AppNavComponent`) continua, tal como desde a Fase 1, a mostrar o item "Administração" a qualquer utilizador autenticado independentemente da função — não há ocultação de itens de navegação por função nesta aplicação (decisão pré-existente, coerente com o princípio do `project-spec.md` de que "a ocultação de elementos de interface no frontend não constitui, por si só, um mecanismo de controlo de segurança"); a proteção real é inteiramente feita pelo `roleGuard`, confirmada a funcionar corretamente;
  - commit efetuado em `feature/fase-9-ui-administracao` ("Fase 9 (UI) — Administração, com migração da Fase 2 para múltiplas funções por utilizador"), a pedido do utilizador.
- branch `feature/fase-9-ui-administracao` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; `lint`, `typecheck`, `build` e `test` (327 testes, 323/327 numa execução limpa) revalidados em `main` após o merge — as 4 falhas são a mesma intermitência por timeout, não determinística e não relacionada com esta fase, já documentada desde a Fase 4 (desta vez em `resource-table` e `taxonomy-management`); nenhuma alteração de código necessária além da já registada acima.
- Criação da especificação da Fase 10 (UI) — Página Inicial (`context/features/fase-10-ui-pagina-inicial.md`), inserida depois de a Fase 1 ter criado apenas um *placeholder* para `/inicio`, já que a página inicial completa depende de dados que só existem a partir da Fase 3 (recursos) e da Fase 6 (pedidos de suporte); assume como ponto de partida a Fase 1 (Design System), a Fase 2 (Autenticação), a Fase 3 (Catálogo), a Fase 4 (Detalhe de recurso) e a Fase 6 (Suporte — pedidos do trabalhador); a fase seguinte (Acessibilidade, responsividade e testes E2E de UI, que fecha a via de UI) passa a ser a Fase 11; implementação ainda não iniciada.
- Início da Fase 10 (UI) — Página Inicial, na branch `feature/fase-10-ui-pagina-inicial`.
- Implementação completa da Fase 10 (UI), validada manualmente no browser:
  - `ResourceMockService` (Fase 3/8) estendido com `listFeatured(limit)`/`listRecent(limit)` e um novo método privado `visibleResources()` (extrai a filtragem por `isVisible` já usada por `search`/`getBySlug`/`getRelated`, reaproveitada também pelos dois métodos novos); decisão registada (risco em aberto do `fase-10-ui-pagina-inicial.md`): sem campo explícito `isFeatured` no modelo `Resource` nem nos dados mock, "recursos em destaque" usa os publicados mais recentemente (`publishedAt`), propositadamente distinto de "recursos recentes" (`updatedAt`), para que as duas secções nunca fiquem idênticas; testado exaustivamente (ordenação, limite, visibilidade por estado editorial e função);
  - três componentes novos em `features/home`, cada um um widget independente (mesmo padrão já usado pela Fase 9 em `AdministrationPageComponent`: `UserTableComponent`/`TaxonomySummaryComponent`/`AuditLogListComponent`), com o próprio `toSignal`/skeleton/estado vazio, para que a demora simulada de um nunca bloqueie os outros dois:
    - `FeaturedResourcesComponent` — grelha compacta de `ResourceCardComponent` (Fase 1/3, sem duplicar nem criar uma variante "compacta"), a partir de `listFeatured`;
    - `RecentResourcesComponent` — lista compacta (não a grelha de cartões) a partir de `listRecent`, cada item a navegar para `/recursos/:slug`;
    - `MyOpenTicketsComponent` — lista compacta dos pedidos do utilizador atual com estado diferente de "Encerrado" (reaproveitando `SupportTicketMockService.listMine()`, Fase 6, filtrando no `computed`), com `TicketReferenceComponent`/`fdr-tag` (mesmo padrão de tom da Fase 6/7) e atalho "Ver todos" para `/suporte`; estado vazio com mensagem positiva ("Não tem neste momento nenhum pedido de suporte pendente"), não um erro, conforme exigido;
  - `HomePageComponent` (Fase 1, até agora um *placeholder*) reescrito por completo: *hero* com o texto exato do `project-spec.md` (título e subtítulo, sem paráfrase) e as duas ações ("Ver recursos" → `/recursos`, "Pedir suporte" → `/suporte/novo`); três cartões de funcionalidades estáticos (Vídeos formativos, Guias em PDF, Pedidos de suporte), cada um a navegar — decisão registada, já que a especificação não fixava o destino: Vídeos → `/recursos?tipo=video`, Guias → `/recursos?tipo=guia` (reaproveitando os valores de URL já definidos pelo catálogo desde a Fase 3), Pedidos de suporte → `/suporte`; pesquisa rápida com `FormGroup`/`FormControl` (Reactive Forms, um único campo) que apenas inicia a navegação para `/recursos?q=...` — a pesquisa em si continua a ser feita exclusivamente pelo catálogo, sem duplicar lógica; junta os três widgets independentes acima;
  - bug evitado por precedente já documentado (Fase 8): o formulário de pesquisa rápida usa `[formGroup]`+`(ngSubmit)` (não um `FormControl` isolado sem `[formGroup]`), pela mesma razão já registada nessa fase — sem `[formGroup]`, `(ngSubmit)` nunca dispara;
  - falso positivo do linter estático já documentado desde a Fase 2 (`AppNavComponent`) repetido nesta fase em três `<ul>` com `@for` (feature cards, recursos recentes, meus pedidos) — confirmado via axe-core/inspeção em runtime que cada `<ul>` só contém `<li>`; mantido `@for`, sem alteração de código, apenas com o mesmo comentário explicativo já usado na Fase 2;
  - validação automática: `lint`, `typecheck`, `test` (348 testes, incluindo os novos `FeaturedResourcesComponent`, `RecentResourcesComponent`, `MyOpenTicketsComponent`, `HomePageComponent` e os testes novos de `listFeatured`/`listRecent` em `ResourceMockService`) e `build` (produção) todos a passar sem erros; `format:check` sem regressões nos ficheiros desta fase (o aviso pré-existente por fim de linha `core.autocrlf`, documentado desde a Fase 2, continua a afetar apenas ficheiros não tocados por esta fase); uma primeira execução da suite mostrou 1 falha por timeout em `taxonomy-management.component.spec.ts` (Fase 8, não relacionada), confirmada como a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4 — uma segunda execução limpa passou 348/348;
  - ferramenta de validação manual desta fase: sem `chromium-cli` disponível nesta sessão; `puppeteer-core` instalado apenas na pasta de scratchpad (fora do projeto, nunca adicionado a `package.json`), a controlar Microsoft Edge local contra uma segunda instância de `ng serve` (porta alternativa, para não interferir com uma instância já em execução noutra sessão), autenticando via ferramenta de simulação de função e navegando por cliques reais dentro da SPA;
  - validação manual completa: *hero* com o texto exato e as duas ações a navegar corretamente; três cartões de funcionalidades presentes e a navegar para os destinos corretos; pesquisa rápida a navegar para `/recursos?q=...`; "Recursos em destaque" e "Recursos recentes" a mostrar 4 itens cada (Marta Silva, `EMPLOYEE`), sem nenhum rascunho; "Os meus pedidos" a mostrar exatamente os 4 pedidos abertos de Marta Silva (`sup-1` a `sup-4`), nunca o pedido "Encerrado" (`sup-5`) nem pedidos de outros utilizadores mock; sem overflow horizontal nas 5 larguras de referência (320/375/768/1024/1440 px, confirmado por `scrollWidth`/`clientWidth`); inspeção visual em modo claro e escuro sem problemas aparentes; zero erros de consola em toda a validação;
  - nota metodológica confirmada nesta fase (mesma limitação já documentada desde a Fase 3): a verificação a 375 px teve de reentrar em `/inicio` por navegação interna da SPA (clique no item de navegação "Início"), não por `page.goto` direto — uma primeira tentativa com `page.goto` perdeu a sessão em memória e mostrou o ecrã de login, confirmando mais uma vez que só a navegação interna preserva a sessão simulada;
  - commit efetuado em `feature/fase-10-ui-pagina-inicial` ("Fase 10 (UI) — Página Inicial"), a pedido do utilizador.
- branch `feature/fase-10-ui-pagina-inicial` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; `lint`, `typecheck` e `build` revalidados em `main` após o merge, todos a passar sem erros; `test` (348 testes) revalidado em várias execuções limpas, com falhas intermitentes por timeout em ficheiros distintos e não relacionados a cada execução (`taxonomy-management`, depois `content-management-page`) — a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4, nunca ocorrendo em nenhum teste desta fase; nenhuma alteração de código necessária além da já registada acima.
- Criação da especificação da Fase 11 (UI) — Acessibilidade, Responsividade e Testes E2E de UI (`context/features/fase-11-ui-acessibilidade-e2e.md`), a décima primeira e última fase da via de UI: não introduz ecrãs novos, consolida e audita as Fases 1 a 10 como um todo coerente (axe-core em todas as rotas/funções, navegação só por teclado, as 5 larguras de referência, contraste AA em ambos os temas, `prefers-reduced-motion`, zoom a 200%, alvos táteis), acrescenta a suite de testes E2E (Playwright) dos catorze fluxos mínimos de `project-spec.md` sobre os serviços mock, e produz o documento de referência das interfaces dos serviços mock (`ResourceMockService`, `AuthService`, `SupportTicketMockService`, `TipMockService`/`FaqMockService`, `TaxonomyMockService`, `UserMockService`, `AuditLogMockService`) como contrato para a via de integração com a API real, que retoma depois desta fase a partir de `fase-1-fundacao.md`; implementação ainda não iniciada.
- Início da Fase 11 (UI) — Acessibilidade, Responsividade e Testes E2E de UI, na branch `feature/fase-11-ui-acessibilidade-e2e`.
- Implementação completa da Fase 11 (UI), primeira fase a introduzir Playwright como dependência real do projeto (`@playwright/test`, `@axe-core/playwright`, ambos em `devDependencies`; até aqui, Playwright/Puppeteer só tinham sido usados como ferramentas de validação manual temporárias em scratchpads, nunca comitadas):
  - `playwright.config.ts` novo (raiz do projeto): suite em `e2e/`, `webServer` a correr `ng serve --configuration production` numa porta dedicada (4310) — decisão registada do risco em aberto da especificação: build de produção servida localmente, "mais fiel ao comportamento final", em vez do modo de desenvolvimento; `workers: 1` (estado em memória dos serviços mock e sessão simulada tornam paralelismo entre testes desnecessariamente arriscado); novo script `test:e2e` em `package.json`;
  - `e2e/fixtures/` novo: `users.ts` (ponte para `mock-credentials.ts`/`users.mock.ts` reais, nunca duplicados — `testUsers` com um utilizador de referência por função de base, mais o caso de duas funções e o segundo agente de suporte; `roleAudienceForRoute`, mapa rota→funções relevantes para a tarefa A); `routes.ts` (slugs/ids de dados mock já existentes, reutilizados como parâmetros de rota); `auth.ts` (`login` via formulário real, não a ferramenta de simulação de função — para que o fluxo 1 exercite o mesmo caminho que um utilizador real; `logout`; `spaNavigate`, que dispara `history.pushState` + `popstate` manualmente, formalizando como *helper* reutilizável a técnica já usada informalmente nas validações manuais das Fases 3–10 para nunca perder a sessão simulada em memória por uma navegação de browser real); `axe.ts` (`scanForBlockingViolations`, filtra por impacto crítico/sério, tags `wcag2a`/`wcag2aa`/`wcag21aa`);
  - **tarefa A** (`e2e/a11y/routes-axe.spec.ts`): axe-core contra as catorze rotas mínimas da especificação, para cada função relevante (`/login` não autenticado; `/inicio` nas 4 funções de base; restantes rotas conforme `roleAudienceForRoute`; variantes de vídeo/guia de `/recursos/:slug`; `/suporte/:id`; `/acesso-negado`; `/**`); zero violações críticas/sérias em todas — depois de corrigidos os bugs reais abaixo;
  - **tarefas B–E** (`e2e/audits/`): `keyboard-navigation.spec.ts` (link "saltar para o conteúdo" como primeiro elemento focável e a mover o foco por teclado real — Tab a partir do topo da página, não `.focus()` programático nem `.click()`, este último inválido para um link só visível quando focado; ausência de `tabindex` positivo; gaveta de navegação móvel; dropdown de filtro; acordeão de FAQ; diálogo de confirmação com foco contido e devolvido); `responsive.spec.ts` (as 5 larguras de referência em todos os ecrãs relevantes, sem `scrollWidth > innerWidth`; gaveta de navegação móvel a 375 px em várias rotas); `theme-contrast.spec.ts` (axe-core no tema escuro para uma amostra representativa de rotas — a tarefa A já cobre o tema claro, por ser o tema por omissão do browser nesta suite; ausência de "flash" de tema incorreto, com preferência guardada e sem preferência guardada, confirmada ao nível do script inline de `index.html`, antes do arranque do Angular); `motion-zoom-touch.spec.ts` (`prefers-reduced-motion` reduz `--fdr-duration-base`/a transição do acordeão a `0`; 200% de zoom simulado por viewport reduzida a metade, sem overflow horizontal; alvos táteis ≥44×44 px a 375 px);
  - **tarefa F** (`e2e/flows/`, 7 ficheiros): os catorze fluxos mínimos de `project-spec.md`, cada um isolado por omissão pelo Playwright (contexto de browser novo por teste, logins sempre a partir do zero) — login/pesquisa/filtros; abertura de vídeo/PDF; criação/consulta/resposta de ticket (trabalhador); resposta/nota interna/resolução (agente); publicação de recurso (edita `res-3`, rascunho já com `pdfUrl` definido nos dados mock, para não depender de um carregamento de ficheiro real); gestão de utilizador (atribui uma função adicional a Sofia Ramos); bloqueio de acesso indevido (EMPLOYEE→`/administracao`; utilizador a aceder ao pedido de outro);
  - **tarefa G**: `docs/interfaces-mock-ui.md` novo, com a assinatura pública dos 7 serviços mock da via de UI, produzida por leitura direta do código-fonte de cada serviço (não por memória/suposição), incluindo os métodos síncronos e as respetivas notas para a via de integração;
  - `.github/workflows/ci.yml` novo — nenhum pipeline de CI existia ainda no repositório, apesar de referido como já existente desde a Fase 1 em fases anteriores da especificação; corre `lint`, `format:check`, `typecheck`, `test`, `test:e2e` (com `playwright install --with-deps chromium`) e `build`, nesta ordem, em `push`/`pull_request` para `main`;
  - **4 bugs reais encontrados e corrigidos**, todos em componentes partilhados (nunca ecrã a ecrã, conforme o risco em aberto da especificação), nenhum detetável pelos testes unitários existentes (todos de integração real em browser):
    1. **contraste insuficiente sistemático** — `--fdr-accent-primary` (tom `--fdr-laranja-500`) usado como fundo sólido com texto branco (`fdr-button--primary`, `fdr-chip--selected`, contador do `fdr-dropdown-filter`, página atual da `fdr-pagination`, `fdr-segmented__option--selected`, link "saltar para o conteúdo") só atinge 3.12:1, abaixo do mínimo AA de 4.5:1 — o mesmo problema já identificado e contornado *localmente* no avatar do `UserMenuComponent` desde a Fase 2, nunca corrigido na origem; corrigido introduzindo dois tokens semânticos novos e theme-independentes, `--fdr-accent-primary-solid`/`-solid-hover` (`--fdr-laranja-700`/`-900`, ambos com margem de segurança generosa acima de 7:1 — medições reais em Chromium via axe-core mostraram desvio por antialiasing acima do calculado nominalmente, exigindo mais margem do que os ~4.9–5.7:1 tentados inicialmente), usados nos 6 componentes partilhados acima; `--fdr-accent-primary` (papel de texto/ícone/contorno sobre fundo, não de preenchimento) passou a divergir por tema — mantido em `--fdr-laranja-500` no tema escuro (já suficiente contra fundos escuros), alterado para `--fdr-laranja-700` no tema claro (o link "Gerir →" do `TaxonomySummaryComponent` também só atingia 3.12:1 no tema claro); adicionalmente, `--fdr-tone-warning-text` (etiquetas de prioridade "Alta"/dificuldade "Intermédia") media 4.07:1 no tema claro — novo primitivo `--fdr-ambar-800`, ~5:1;
    2. **link "saltar para o conteúdo" totalmente inoperante** (não detetado em nenhuma validação manual das Fases 1–10) — com `<base href="/">` em `index.html` (necessário para o `Router`), um `<a href="#fdr-main-content">` resolve o fragmento contra a *base*, não contra a rota atual: ativá-lo a partir de qualquer rota que não seja exatamente `/` navega para `/#fdr-main-content`, uma mudança de caminho real (não apenas de fragmento), que recarrega a aplicação e perde a sessão simulada em memória — o utilizador acaba, sem qualquer indicação, de volta no ecrã de login; corrigido no `AppShellComponent` com `(click)="focusMainContent($event)"` que chama `event.preventDefault()` e move o foco por JavaScript (`document.getElementById(...).focus()`), sem depender da navegação nativa do browser;
    3. **overflow horizontal de ~12 px a 320 px no cabeçalho** (`AppHeaderComponent`, partilhado por toda a aplicação autenticada) — com a pesquisa já escondida abaixo dos 767 px (Fase 1), a marca, o botão de gaveta e as duas ações (tema/menu do utilizador) ainda não cabiam com o preenchimento (`--fdr-space-5`) e espaçamento (`--fdr-space-4`) pensados para ecrãs largos; corrigido reduzindo ambos para `--fdr-space-3`/`--fdr-space-2` abaixo dos 767 px;
    4. **botão de resguardo ("scrim") de diálogos alcançável e por vezes autofocado antes do conteúdo real** — o `<button class="fdr-dialog-scrim">` do `DialogComponent` partilhado, sem `tabindex="-1"`, era candidato à captura automática de foco `autoFocus: 'first-tabbable'` do `Dialog` do Angular CDK (`DialogService`), fazendo o primeiro `Tab`/o foco inicial de qualquer diálogo (incluindo `ConfirmDialogComponent`, usado em ações sensíveis como arquivar um recurso ou eliminar uma taxonomia) pousar num botão de ecrã inteiro sem qualquer rótulo visível em vez de no primeiro controlo real do diálogo; corrigido com `tabindex="-1"` no próprio `.fdr-dialog-scrim`;
  - **alvos táteis**: 3 correções adicionais no viewport móvel (≤767 px) para o mínimo indicativo de 44×44 px, sem alterar o tamanho mais compacto em ecrã largo: botões de ícone do cabeçalho (gaveta/tema), botão de fechar do `DialogComponent`, botões de navegação da `PaginationComponent`;
  - validação automática completa: `lint`, `format:check` (sem regressões nos ficheiros desta fase — o aviso pré-existente por fim de linha `core.autocrlf`, documentado desde a Fase 2, continua a afetar apenas ficheiros não tocados por esta fase, confirmado por comparação de `git diff --stat` contra `main`), `typecheck`, `test` (348 testes; uma execução mostrou 1 falha por timeout em `taxonomy-management.component.spec.ts`, confirmada por reexecução isolada, em 7.6 s, como a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4) e `build` (produção) todos a passar sem erros;
  - `test:e2e`: suite completa (58 testes — 21 da tarefa A, 15 das tarefas B–E, 15 dos catorze fluxos, mais duas variantes de rota) a passar de forma consistente e repetida em várias execuções completas, incluindo depois de cada correção de bug; nenhuma instabilidade residual aceite sem correção, conforme exigido pelos critérios de aceitação da especificação — as únicas falhas ao longo da implementação foram diagnosticadas e corrigidas uma a uma (seletores de teste incorretos por papel ARIA/nome de classe, ou os 4 bugs reais documentados acima), nunca "resolvidas" por repetição automática;
  - commit efetuado em `feature/fase-11-ui-acessibilidade-e2e` ("Fase 11 (UI) — Acessibilidade, Responsividade e Testes E2E de UI"), a pedido do utilizador.
- branch `feature/fase-11-ui-acessibilidade-e2e` integrada em `main` por pedido do utilizador (merge de integração, sem squash); `lint`, `typecheck`, `build` e `test` (348/348, sem intermitências desta vez) revalidados em `main` após o merge, todos a passar sem erros; nenhuma alteração de código necessária além da já registada acima.

## Fecho da via de UI

Com a Fase 11 concluída e integrada em `main`, a via de desenvolvimento do UI
(Fases 1 a 11) está terminada: aplicação Angular completa, navegável, acessível e
responsiva, com todas as áreas previstas em `project-overview.md`, a funcionar
inteiramente sobre dados e serviços simulados, e com uma suite E2E (Playwright) e um
pipeline de CI a validar esse estado de forma repetível.

- As especificações da via de UI (Fases 1 a 11) foram reorganizadas para `context/features/ui/`. O trabalho segue agora para a via de **Base de Dados e Deployment Inicial**, com as respetivas especificações em `context/features/db_ci_cd/`, começando por `context/features/db_ci_cd/fase-1-bd-modelo-dados.md` (Fase 1 (BD) — Modelo de Dados Completo: inicialização do Prisma, ligação ao PostgreSQL de desenvolvimento e o modelo de dados completo — todas as entidades, enums, relações e migrações de `project-spec.md`); implementação ainda não iniciada.

## Via de Base de Dados e Deployment Inicial

- Início da Fase 1 (BD) — Modelo de Dados Completo, na branch `feature/fase-1-bd-modelo-dados`.
- Reestruturação do repositório para monorepo (npm workspaces), pedida explicitamente pelo utilizador depois de confirmado que `coding-standards.md`/`project-spec.md` já assumiam `apps/web`+`apps/api` mas o repositório ainda tinha a app Angular na raiz: `src/`, `public/`, `e2e/`, `angular.json`, `tsconfig*.json`, `eslint.config.js`, `playwright.config.ts` e o `README.md` original movidos para `apps/web/` (via `git mv`, sem alterações internas — todos os caminhos já eram relativos); `package.json` raiz novo, só orquestrador (`workspaces: ["apps/web", "apps/api"]`, scripts que delegam com `--workspace`); `.gitignore` desancorado da raiz (`dist/`, `node_modules/`, etc., sem `/` inicial) para cobrir ambos os workspaces, mais bloco `.env`/`.env.example`; novo `README.md` de raiz com visão geral do monorepo; `.vscode/tasks.json`/`launch.json` (`npm: start`, `npm: test`) mantidos sem alterações, já que os scripts `start`/`test` da raiz continuam a existir, agora delegando para `apps/web`;
- `apps/api` criado com `@nestjs/cli new` (scaffold padrão, sem lógica de negócio nova — fora de âmbito desta fase); `tsconfig.json` ajustado para `strict: true` (conforme `.claude/CLAUDE.md`), removendo os relaxamentos que o scaffold trazia por omissão; scripts `format:check`/`typecheck`/`prisma:*` acrescentados ao `apps/api/package.json`, e `lint` alterado para não usar `--fix` automático (mesmo padrão de verificação, não correção silenciosa, usado por `apps/web`);
- `docker-compose.yml` mínimo (raiz): um único serviço `postgres:16-alpine`, com *health check* e volume persistente, conforme o âmbito explícito da especificação (sem MinIO/API/frontend em contentor); porta do anfitrião mapeada para `5433` (não `5432`) — descoberto durante a validação que este ambiente já tinha um PostgreSQL nativo do Windows a ocupar a porta 5432 por omissão, o que causava falhas de autenticação silenciosas ao ligar via `localhost:5432` (o contentor arrancava e ficava saudável, mas as ligações TCP do anfitrião eram na realidade servidas pela instância nativa); resolvido sem tocar nessa instância nativa, só mudando o mapeamento de porta do `docker-compose.yml` e de todas as referências (`apps/api/.env(.example)`, `README.md`); a `ci.yml` mantém a porta 5432 (runner efémero do GitHub Actions, sem conflito);
- `apps/api/src/config/env.validation.ts`: função `validate` própria (sem Joi/Zod — decisão registada em `docs/decisoes-modelo-dados.md`, por só ser necessário validar uma variável obrigatória nesta fase) ligada a `ConfigModule.forRoot({ isGlobal: true, validate })` em `AppModule`; rejeita o arranque sem `DATABASE_URL`, confirmado tanto por teste direto da função como pela aplicação completa (compilada e corrida com `node dist/main.js`) a arrancar com sucesso e a ligar-se à base de dados via Prisma quando `DATABASE_URL` está definida;
- `apps/api/src/prisma/`: `PrismaService` (`extends PrismaClient`, liga/desliga em `onModuleInit`/`onModuleDestroy`) e `PrismaModule` (`@Global()`), centralizando o ciclo de vida do Prisma Client conforme `coding-standards.md`;
- `apps/api/prisma/schema.prisma`: modelo de dados completo das 13 entidades de `project-spec.md` (`USER`/`USERROLE`/`SESSION`, `RESOURCE`/`WORKFLOW`/`DOCUMENTTYPE`/`TAG`/`RESOURCETAG`, `TIP`/`FAQ`, `SUPPORTTICKET`/`TICKETMESSAGE`/`TICKETATTACHMENT`, `AUDITLOG`), com os 10 enums da especificação (`ResourceStatus`/`ContentStatus` mantidos distintos, apesar de partilharem os 3 valores, para não acoplar os dois ciclos editoriais); `id` em todas as entidades via `cuid()`; todos os campos `DateTime` com `@db.Timestamptz(3)` explícito (o mapeamento por omissão do Prisma para PostgreSQL não inclui fuso horário); regra geral de `onDelete: Restrict` para relações obrigatórias associadas a `User`/taxonomias (citando literalmente a regra "eliminação restrita quando existirem relações relevantes associadas" de `project-spec.md`), com exceções documentadas caso a caso (`User→UserRole`/`Session` em `Cascade`; `AuditLog.actorId` em `SetNull`, exceção deliberada para sobreviver à eliminação da conta; `assigneeId`/`relatedResourceId` em `SetNull`, por serem atribuições substituíveis, não factos históricos); índices para todos os campos de pesquisa/filtro/ordenação identificados em `project-spec.md`, mais um conjunto adicional justificado em `docs/decisoes-modelo-dados.md` (incluindo índices em colunas de chave estrangeira, que o Prisma não cria automaticamente no PostgreSQL, ao contrário do MySQL);
- 5 migrações Prisma versionadas, uma por grupo coerente, com os nomes já sugeridos na especificação (`add_user_roles`, `add_resource_taxonomies`, `add_tips_faqs`, `add_support_tickets`, `add_audit_log`), todas geradas e aplicadas com `prisma migrate dev` sobre o PostgreSQL real do `docker-compose.yml` (sem `prisma db push` em momento algum); a "primeira migração vazia" da tarefa A da especificação não chegou a produzir uma pasta de migração (o diff entre um schema sem modelos e uma base de dados vazia é vazio, pelo que o Prisma não tem nada para gerar), mas confirmou a ligação com sucesso, que era o objetivo dessa tarefa;
- `docs/decisoes-modelo-dados.md` novo, com a justificação de cada decisão acima (estratégia de IDs, timestamps, regra geral e exceções de `onDelete`, separação dos dois enums de estado editorial, geração aplicacional da referência do ticket, índices adicionais, ausência de biblioteca de validação de esquema, unicidade de `Session.tokenHash`);
- validação completa: `prisma format`/`prisma validate` sem alterações pendentes; `prisma migrate status` sem divergências (5 migrações, tudo sincronizado); unicidade confirmada por inspeção direta da base de dados (`\d`) para `User.email`, `Resource.slug`, `SupportTicket.reference` e as chaves compostas de `UserRole`/`ResourceTag`; `apps/api`: `lint`, `format:check` (depois de normalizar com `prettier --write` os ficheiros do scaffold do Nest, que tinham fim de linha CRLF por omissão do `core.autocrlf` neste ambiente Windows — mesma causa raiz já documentada desde a Fase 2 da via de UI, desta vez em ficheiros novos desta fase, por isso corrigida em vez de só anotada), `typecheck` e `build` todos a passar sem erros; `apps/web`, revalidado depois da reestruturação: `lint`, `format:check` (mesmo aviso pré-existente por `core.autocrlf`, já documentado desde a Fase 2, sem regressões novas), `typecheck` e `build` (produção) sem erros; `test` (348 testes) com a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4 (desta vez em `content-management-page`/`resource-table`/`taxonomy-management`, variando entre execuções, incluindo uma execução isolada com falha generalizada por sobreposição de dois processos `ng test` em simultâneo) — não relacionada com a reestruturação; `test:e2e` (Playwright, 57/57) a passar de forma limpa contra a build de produção servida a partir de `apps/web`, confirmando que a via de UI continua inteiramente funcional depois da mudança de estrutura do repositório;
- corrigido, durante a validação: aviso `@typescript-eslint/no-floating-promises` em `apps/api/src/main.ts` (`bootstrap()` → `void bootstrap()`), único aviso de lint do scaffold gerado pelo Nest CLI;
- corrigido, também durante a validação, um bug real no `.gitignore` encontrado ao rever o `git status` antes do commit: o padrão `.angular/cache/` (e, pelo mesmo motivo, `playwright/.cache/`) tem uma barra no meio, o que o Git ancora sempre à raiz do repositório independentemente de haver ou não barra inicial — não cobria `apps/web/.angular/cache/` depois da reestruturação para monorepo; corrigido para `**/.angular/cache/` e `**/playwright/.cache/`, confirmado com `git check-ignore -v`;
- commit efetuado em `feature/fase-1-bd-modelo-dados` ("Fase 1 (BD) — Modelo de Dados Completo"), a pedido do utilizador.
- branch `feature/fase-1-bd-modelo-dados` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; sem revalidação adicional de `lint`/`typecheck`/`test`/`build` após o merge — ao contrário das fases da via de UI, `main` não tinha nenhum commit novo desde a criação da branch, pelo que a árvore resultante do merge é idêntica, ficheiro a ficheiro, à da branch já validada por completo antes do commit.
- Criação da especificação da Fase 2 (BD) — Seeds e Validação de Dados (`context/features/db_ci_cd/fase-2-bd-seeds-validacao.md`), assumindo como ponto de partida a Fase 1 (BD): seeds idempotentes de taxonomias, utilizadores, recursos/dicas/FAQ e pedidos de suporte, testes de integração das queries reais de catálogo/tickets/utilizadores (incl. prevenção de N+1) e verificação prática das restrições de negócio críticas da Fase 1; implementação ainda não iniciada.
- Início da Fase 2 (BD) — Seeds e Validação de Dados, na branch `feature/fase-2-bd-seeds-validacao`.
- Implementação completa da Fase 2 (BD):
  - novas dependências em `apps/api`: `@node-rs/argon2` (não `argon2` — binários pré-compilados, incluindo Windows, sem `node-gyp`), `nanoid@3` (não a v6, ESM-only, incompatível com o projeto CommonJS/`module: nodenext`), `dotenv`;
  - `src/auth/password.util.ts` (`hashPassword`/`verifyPassword`, Argon2id), `src/support/ticket-reference.util.ts` (`generateTicketReference`/`generateUniqueTicketReference`/`isValidTicketReferenceFormat`, mesmo alfabeto/comprimento já usado por `SupportTicketMockService` na via de UI, Fase 6 — "para não divergir entre as duas implementações") e `src/users/admin-guard.util.ts` (`assertCanRemoveAdminRole`/`LastAdminError`, regra do último `ADMIN` como função pura, ainda sem camada de serviço nesta via), todos com testes unitários isolados (20 testes novos);
  - `apps/api/prisma/seed-data/` (dados tipados: `taxonomies.data.ts`, `users.data.ts`, `resources.data.ts`, `tips.data.ts`, `faqs.data.ts`, `support-tickets.data.ts`, `slugify.ts`) e `apps/api/prisma/seeds/` (uma função por grupo de entidades, todas com `upsert`), orquestrados por `apps/api/prisma/seed.ts` (`runSeed`, exportado para reutilização pelos testes de integração; bloqueia a execução com `NODE_ENV=production`);
  - dados de seed reutilizados tal e qual dos mocks da via de UI (títulos/resumos/descrições de `resources.mock.ts`, identidades de `users.mock.ts`/`mock-credentials.ts`, `tips.mock.ts`, `faqs.mock.ts`, `support-tickets.mock.ts`): 6 utilizadores (cobrindo as 4 funções, um com duas funções em simultâneo — `João Antunes`, `CONTENT_EDITOR`+`ADMIN` —, dois `ADMIN` e um utilizador inativo), 8 fluxos, 7 tipos de documento, 12 etiquetas genéricas (subconjunto das mais de 40 do mock, "genéricas" o suficiente para uma taxonomia inicial), 24 recursos (16 publicados/5 rascunho/3 arquivados, com referências simbólicas de ficheiro — sem armazenamento real), 6 dicas, 7 perguntas frequentes (a sétima nova nesta fase, para cobrir uma terceira categoria) e 8 pedidos de suporte (o oitavo novo nesta fase, categoria `OTHER`, com histórico completo: criação, mensagem pública do trabalhador, nota interna do agente, resposta pública do agente, resolução e confirmação);
  - decisões registadas em `docs/decisoes-seeds.md`: chave de `upsert` por entidade (campos já únicos onde existem; `id` determinístico prefixado `seed-` para `Tip`/`Faq`/`TicketMessage`, que não têm outro campo único), reatribuição da autoria de recursos/dicas/FAQ a utilizadores com função de edição de conteúdo (o mock de UI não validava o `author` contra a função), título novo para `Tip` (o mock só guardava uma frase única; o modelo desta via tem `title`+`content` distintos), geração real da referência de ticket (utilitário) vs. referências fixas nos dados de seed (idempotência exige uma chave estável);
  - base de dados de testes de integração isolada: novo serviço `postgres-test` em `docker-compose.yml` (perfil `test`, porta 5434, sem volume — `docker compose --profile test up -d postgres-test`), `apps/api/.env.test.example`, `apps/api/test/integration/` (config Jest dedicada, `setupFiles`/`globalSetup` que aplicam migrações e semeiam a base de dados uma única vez antes de toda a suite);
  - bug de ordem de carregamento de ambiente encontrado e corrigido durante a implementação: `prisma/seed.ts` (efeito secundário `import 'dotenv/config'`) e `@prisma/client` (resolve a `DATABASE_URL` já no próprio import) carregavam `.env` (base de dados de desenvolvimento) antes de `global-setup.ts` conseguir carregar `.env.test`, tanto com `import` estático (içado para o topo do ficheiro) como com `import()` dinâmico (resolvido pelo carregador ESM nativo do Node, fora do alcance de `ts-jest`, e por isso incapaz de resolver módulos `.ts` sem extensão); corrigido carregando os dois módulos com `require` dentro da própria função de setup, depois do `dotenv.config`;
  - 32 testes de integração novos (`apps/api/test/integration/`): `catalog.integration-spec.ts` (pesquisa por texto sem distinção de maiúsculas, nunca devolvendo rascunhos/arquivados; filtros combinados por tipo/dificuldade/fluxo; paginação `skip`/`take` com total real; ordenação por data de publicação e alfabética; prevenção de N+1 confirmada por contagem real de queries via `$on('query', ...)`, com um teste de contraste que reproduz deliberadamente o padrão N+1 ingénuo para mostrar que escala com o número de resultados, ao contrário do `include` do Prisma), `tickets.integration-spec.ts` (vista do trabalhador nunca devolve pedidos de outro utilizador, nem distingue um id inexistente de um pedido alheio; vista do agente com filtros combinados; notas internas nunca aparecem numa consulta pública), `users.integration-spec.ts` (listagem por função via join com `UserRole`, com o utilizador de duas funções a aparecer corretamente em ambos os filtros), `business-rules.integration-spec.ts` (eliminação de `Workflow`/`DocumentType`/`Tag` em uso bloqueada com o código real do Prisma `P2003`; `slug`/`reference` duplicados bloqueados com `P2002`; regra do último `ADMIN` confirmada reduzindo temporariamente os `ADMIN` do seed a um só, com reposição do estado no `finally`) e `seed-idempotency.integration-spec.ts` (segunda execução do seed sem alterar contagens; cobertura de todos os estados/dificuldades/categorias/prioridades; nenhuma palavra-passe em texto simples);
  - `.github/workflows/ci.yml` estendido: `apps/api` passou a correr também `test` (unitários), o seed **duas vezes seguidas** (prova de idempotência) e `test:integration`, antes do `build` já existente;
  - `README.md` e `docs/decisoes-seeds.md` atualizados/criados com os novos comandos (`prisma:seed`, `test:integration`, arranque da base de dados de testes);
  - validação completa e repetida: `apps/api` — `lint`, `format:check`, `typecheck`, `test` (20/20), `test:integration` (32/32, em várias execuções consecutivas, incluindo a partir de uma base de dados de testes vazia) e `build`, todos sem erros; `prisma db seed` corrido duas vezes seguidas contra a base de dados de desenvolvimento, sem erros e sem duplicar nenhuma entidade (contagens confirmadas por query direta: 6 utilizadores, 7 `UserRole`, 8 fluxos, 7 tipos de documento, 12 etiquetas, 24 recursos, 26 `ResourceTag`, 6 dicas, 7 FAQ, 8 tickets, 17 mensagens); `apps/web` revalidado por completo (`lint`, `typecheck`, `test`, `build`) apesar de esta fase não o ter alterado — `format:check` manteve o mesmo aviso pré-existente por fim de linha `core.autocrlf` (documentado desde a Fase 2 da via de UI) e uma execução de `test` mostrou 1 falha por timeout em `taxonomy-management.component.spec.ts`, confirmada por reexecução isolada (3/3) como a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4, não relacionada com esta fase;
  - commit efetuado em `feature/fase-2-bd-seeds-validacao` ("Fase 2 (BD) — Seeds e Validação de Dados"), a pedido do utilizador; segundo commit pequeno na mesma branch a corrigir um lapso do primeiro (`ci.yml`, `.gitignore`, `README.md` e `apps/api/package.json` tinham ficado de fora por um pathspec inválido — `apps/api/package-lock.json`, que não existe: o lockfile é único na raiz do monorepo — que abortou silenciosamente esse `git add`).
- branch `feature/fase-2-bd-seeds-validacao` integrada em `main` por pedido do utilizador (merge de integração, sem squash) e apagada de seguida; sem revalidação adicional de `lint`/`typecheck`/`test`/`build` após o merge — tal como na Fase 1 (BD), `main` não tinha nenhum commit novo desde a criação da branch, pelo que a árvore resultante do merge é idêntica, ficheiro a ficheiro, à da branch já validada por completo antes do commit.
- Criação da especificação da Fase 3 (Deployment) — Containerização para Produção (`context/features/db_ci_cd/fase-3-deploy-containerizacao.md`), assumindo como ponto de partida a Fase 1 (BD) e a Fase 2 (BD), já concluídas: `Dockerfile` multi-stage de produção para `apps/api` e `apps/web` (utilizador não-root, `HEALTHCHECK`), estratégia de configuração da Web em tempo de execução do contentor (variáveis vs. ficheiro de configuração gerado no arranque, decisão em aberto com impacto direto na Fase 4), `docker-compose.yml` de referência a subir a stack completa a partir das imagens de produção; implementação ainda não iniciada.
- Início da Fase 3 (Deployment) — Containerização para Produção, na branch `feature/fase-3-deploy-containerizacao`.
- Implementação completa da Fase 3 (Deployment), validada com builds Docker reais (não só leitura de ficheiros):
  - API: novo `HealthController` (`GET /api/v1/health`, verifica ligação real à base de dados via `SELECT 1`) e `HealthModule`, mais `app.setGlobalPrefix('api/v1')` em `main.ts` — nenhum destes existia antes desta fase, apesar de a especificação já pressupor o endereço; `start:prod` corrigido de `node dist/main` (script do scaffold do Nest, nunca antes executado, apontava para um caminho inexistente) para `node dist/src/main` (`outDir` sem `rootDir` explícito faz o `tsc` espelhar `src/` dentro de `dist/`);
  - `apps/api/Dockerfile` multi-stage (`deps` → `build` → `runtime`), imagem final `node:22-slim`, ~460 MB, utilizador não-root `node` (já existente na imagem base, sem criar um novo), sem ferramentas de desenvolvimento nem testes (confirmado por `docker history`); `deps` instala só dependências de produção do workspace `apps/api` (`npm ci --workspace apps/api --omit=dev`), `build` gera o Prisma Client e compila; `runtime` combina os `node_modules` de produção de `deps` com o Prisma Client gerado em `build` (o `npm ci` deste monorepo não hoisteia tudo para a raiz — `@prisma/client` e o cliente gerado ficam em `apps/api/node_modules`, confirmado por inspeção direta, por isso ambos os caminhos são copiados);
  - `apps/api/docker/healthcheck.js`: script Node autónomo (só módulos nativos) para o `HEALTHCHECK`, para não exigir `curl`/`wget` na imagem `slim`;
  - bug real encontrado e corrigido durante a validação (não haveria forma de o prever sem construir a imagem): `node:22-slim` não inclui OpenSSL — sem ele, o motor de consultas do Prisma assumia às cegas `openssl-1.1.x` na *build stage*; instalar OpenSSL só na *runtime stage* (primeira tentativa) piorou o problema, porque o Debian bookworm tem `openssl-3.0.x`, criando um desfasamento entre o motor gerado (`1.1.x`) e a biblioteca disponível no arranque (`3.0.x`), e o contentor falhava logo no arranque (`PrismaClientInitializationError`); corrigido instalando OpenSSL também na *build stage*, antes de `prisma generate`, para o motor gerado corresponder sempre à mesma versão presente em runtime (mesma imagem base nas duas etapas);
  - `apps/web/src/app/core/config/app-config.service.ts` (`AppConfigService`, `providedIn: 'root'`) lê `window.__env.apiUrl`, com `apps/web/public/env.js` como valor por omissão para desenvolvimento local (`ng serve`/build sem contentor); `apps/web/docker/generate-env-config.sh` gera o mesmo ficheiro no arranque do contentor a partir da variável de ambiente `API_URL`, decisão registada nos riscos em aberto da especificação (configuração no arranque do contentor, não em tempo de build, para permitir promover a mesma imagem entre ambientes); `<script src="env.js">` acrescentado ao `<head>` de `index.html`, antes dos scripts do bundle Angular injetados pelo CLI no `<body>`, para garantir que carrega primeiro;
  - `apps/web/Dockerfile` multi-stage (`build` → `runtime`), imagem final `nginxinc/nginx-unprivileged:1.27-alpine` (porta 8080, utilizador não-root `nginx` por omissão da própria imagem), ~49 MB; `apps/web/docker/default.conf` com `try_files ... /index.html` (recarregar uma rota profunda como `/recursos/:slug` não dá 404) e cabeçalhos básicos (`server_tokens off`, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`);
  - bug real encontrado e corrigido: a primeira versão do `Dockerfile` da Web usava `npm ci --workspace apps/web --ignore-scripts` (por analogia com a API, onde `--ignore-scripts` evita disparar prematuramente o `postinstall` do Prisma) — mas o Angular CLI precisa do seu próprio `postinstall` do `esbuild` para resolver o binário nativo da plataforma (`@esbuild/linux-x64`), e o build falhava com `The package "@esbuild/linux-x64" could not be found`; corrigido removendo `--ignore-scripts` do `Dockerfile` da Web (só fazia sentido no da API);
  - segundo bug real encontrado e corrigido: `nginx-unprivileged` corre como utilizador não-root (`nginx`, uid 101), mas um `COPY` sem `--chown` deixa os ficheiros copiados com dono `root`; o `generate-env-config.sh` falhava no arranque (`Permission denied`) ao tentar reescrever `env.js`; corrigido com `COPY --chown=nginx:nginx` no `Dockerfile` da Web — como `env.js` já existe (copiado do build, não é criado de raiz), sobrescrevê-lo só exige permissão de escrita sobre o próprio ficheiro, não sobre o diretório que o contém (que permanece do `root`, herdado da imagem base, sem necessidade de o alterar);
  - `apps/api/Dockerfile.dockerignore` e `apps/web/Dockerfile.dockerignore` (não um `.dockerignore` por pasta — o contexto de build de ambos é sempre a raiz do monorepo, partilhado, e o BuildKit resolve automaticamente `<Dockerfile>.dockerignore` junto do `Dockerfile` referido por `-f`, com precedência sobre um `.dockerignore` genérico); cada um só deixa passar o `package.json` da outra aplicação (necessário para o `npm ci` validar o workspace contra o `package-lock.json` partilhado), excluindo o resto: `node_modules`, `dist`, testes, `.env` reais (mantendo só `.env.example`), documentação;
  - `docker-compose.prod.yml`: PostgreSQL, MinIO (`healthcheck` via `mc ready local`, confirmado por teste direto do binário — vem incluído na imagem oficial), API e Web construídos a partir dos `Dockerfile` de produção, com `depends_on: condition: service_healthy` em cadeia; sem aplicação automática de migrações no arranque da API (decisão deliberada, coerente com `ai-interaction.md` — migrações são um passo manual explícito, documentado no `README.md`);
  - terceiro bug real encontrado e corrigido durante a validação com `docker compose up`: sem um nome de projeto (`name:`) próprio no `docker-compose.prod.yml`, o Compose usa o nome derivado do diretório — o mesmo do `docker-compose.yml` de desenvolvimento — e como os dois ficheiros têm um serviço chamado `postgres`, o `docker compose -f docker-compose.prod.yml up` **recriou o contentor de desenvolvimento**, trocando-lhe o volume de dados (`postgres-data` → `postgres-prod-data`, vazio) por baixo; o volume de desenvolvimento não chegou a ser apagado (confirmado por `docker volume ls` antes de qualquer correção) e foi possível restaurar o contentor de desenvolvimento sem perda de dados (confirmado por `\dt` a mostrar as 15 tabelas depois de restaurado); corrigido com `name: filedoc-prod` no topo do `docker-compose.prod.yml`, para nunca mais colidir com a stack de desenvolvimento — as duas continuam, ainda assim, a partilhar portas do anfitrião (`5433`/`3000`/`8080`) e não podem correr em simultâneo, documentado no `README.md`;
  - validação real de ponta a ponta com `docker compose -f docker-compose.prod.yml up --build`: PostgreSQL/MinIO/API/Web todos "healthy", API a responder em `GET /api/v1/health`, Web a servir `env.js` com o `API_URL` do `docker-compose.prod.yml`, rota profunda da Web a devolver 200 (não 404), base de dados sem tabelas até `prisma migrate deploy` ser corrido manualmente a partir do anfitrião (confirma que as migrações não correm sozinhas); `docker history` de ambas as imagens sem `.env`, testes, ou código-fonte;
  - validação automática pós-implementação: `apps/api` — `lint`, `format:check` (sem regressões; o aviso pré-existente por fim de linha `core.autocrlf`, documentado desde a Fase 2 da via de UI, mantém-se nos mesmos ficheiros não tocados por esta fase), `typecheck`, `test` (22/22, incluindo os 2 novos do `HealthController`) sem erros; `apps/web` — `lint`, `typecheck`, `build` (produção, `env.js` confirmado no `dist/` final) sem erros; `test` (349/350, 1 falha por timeout em `content-management-page.component.spec.ts`, confirmada por reexecução isolada (4/4) como a mesma intermitência não determinística por lentidão da máquina já documentada desde a Fase 4, não relacionada com esta fase);
  - decisão registada: imagem base `node:22-slim` (não `alpine`), testada e preferida por evitar as incompatibilidades conhecidas entre o Prisma e a `musl libc`; a Web usa `nginxinc/nginx-unprivileged:1.27-alpine` (sem Node.js, sem essa restrição, e já não-root por omissão);
  - o scanner de vulnerabilidades do editor assinalou CVEs conhecidas nas imagens base (`node:22-slim`, `nginx-unprivileged:1.27-alpine`) — deixado por resolver nesta fase, por ser uma limitação inerente a qualquer imagem base atual e cair fora do âmbito explícito da especificação ("otimizações avançadas... só se justificam com métricas reais"); a assumir como trabalho de uma fase de segurança dedicada, não desta containerização;
  - `README.md` atualizado com instruções de build/execução de ambas as imagens, da stack completa via `docker-compose.prod.yml`, do passo manual de migrações, e da estratégia de configuração da Web em runtime;
  - commit ainda por autorizar.
